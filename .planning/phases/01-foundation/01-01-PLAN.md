---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/styles/globals.css
  - src/lib/utils.ts
  - components.json
autonomous: true
requirements:
  - FNDN-01
must_haves:
  truths:
    - "Running `npm run dev` starts the development server without errors"
    - "Running `npm run build` completes with zero TypeScript errors"
    - "TypeScript strict mode is enforced (no `any` allowed, strict: true in tsconfig)"
    - "TailwindCSS 4 utility classes render correctly in the browser"
    - "shadcn/ui Button component renders with correct styles"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "next"
    - path: "tsconfig.json"
      provides: "TypeScript strict configuration"
      contains: "strict"
    - path: "next.config.ts"
      provides: "Next.js configuration"
    - path: "src/styles/globals.css"
      provides: "TailwindCSS 4 imports and base styles"
      contains: "@import \"tailwindcss\""
    - path: "src/lib/utils.ts"
      provides: "cn() utility for class merging"
      exports: ["cn"]
    - path: "components.json"
      provides: "shadcn/ui configuration"
      contains: "new-york"
  key_links:
    - from: "src/styles/globals.css"
      to: "tailwindcss"
      via: "@import directive"
      pattern: "@import.*tailwindcss"
    - from: "src/lib/utils.ts"
      to: "clsx + tailwind-merge"
      via: "cn() function"
      pattern: "twMerge.*clsx"
---

<objective>
Scaffold the AceAgency project with Next.js 16 (App Router), TypeScript strict mode, TailwindCSS 4, and shadcn/ui (new-york style).

Purpose: Establish the project skeleton that all subsequent plans build upon. Every dependency must install cleanly and the build must pass before adding i18n or animations.
Output: A working Next.js 16 project with TailwindCSS 4, shadcn/ui initialized, and basic starter components.
</objective>

<execution_context>
@/Users/mihaigrigore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mihaigrigore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project with TypeScript strict mode and TailwindCSS 4</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    src/app/layout.tsx
    src/app/page.tsx
    src/styles/globals.css
  </files>
  <action>
1. Run `npx create-next-app@latest aceagency --typescript --tailwind --eslint --app --turbopack --src-dir` in a temporary location, then move contents into the project root (or run directly if project root is empty enough).

2. Verify `tsconfig.json` has `"strict": true`. Add `"noUncheckedIndexedAccess": true` and `"noImplicitOverride": true` if not already present.

3. Move the default CSS file to `src/styles/globals.css` and update imports in `src/app/layout.tsx` to point to `@/styles/globals.css`. Ensure `globals.css` starts with:
```css
@import "tailwindcss";
@import "tw-animate-css";
```

4. Replace the default `src/app/page.tsx` with a minimal test page:
```tsx
export default function HomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-background text-foreground">
      <h1 className="text-4xl font-bold">AceAgency</h1>
    </main>
  );
}
```

5. Replace the default `src/app/layout.tsx` with a clean shell:
```tsx
import type { Metadata } from 'next';
import '@/styles/globals.css';

export const metadata: Metadata = {
  title: 'AceAgency',
  description: 'Premium digital agency from Bucharest',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ro">
      <body>{children}</body>
    </html>
  );
}
```

6. Run `npm run build` and verify zero errors. Run `npm run dev` and verify the page loads in a browser.
  </action>
  <verify>
- `npm run build` completes with exit code 0
- `grep '"strict": true' tsconfig.json` returns a match
- `grep '@import "tailwindcss"' src/styles/globals.css` returns a match
  </verify>
  <done>Next.js 16 project scaffolded with TypeScript strict mode, TailwindCSS 4, and a clean minimal page that builds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui with new-york style and add base components</name>
  <files>
    components.json
    src/lib/utils.ts
    src/components/ui/button.tsx
  </files>
  <action>
1. Run `npx shadcn@latest init` in the project root. When prompted (or via flags):
   - Style: New York
   - Base color: Neutral
   - CSS variables: Yes
   - Confirm the `components.json` generated has `"style": "new-york"`.

2. Verify `src/lib/utils.ts` was created with the `cn()` function (clsx + tailwind-merge). If not, create it:
```typescript
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]): string {
  return twMerge(clsx(inputs));
}
```

3. Add base shadcn components: `npx shadcn@latest add button card input label separator`

4. Update `src/app/page.tsx` to include a shadcn Button to verify it renders:
```tsx
import { Button } from '@/components/ui/button';

export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 bg-background text-foreground">
      <h1 className="text-4xl font-bold">AceAgency</h1>
      <Button>Get Started</Button>
    </main>
  );
}
```

5. Run `npm run build` to verify shadcn components compile without errors.
  </action>
  <verify>
- `cat components.json | grep new-york` returns a match
- `npm run build` completes with exit code 0
- `ls src/components/ui/button.tsx` exists
- `grep "cn" src/lib/utils.ts` returns a match
  </verify>
  <done>shadcn/ui initialized with new-york style, cn() utility working, Button/Card/Input/Label/Separator components available, build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits with code 0
2. `npm run dev` starts without errors
3. `tsconfig.json` has `"strict": true`
4. `components.json` exists with `"style": "new-york"`
5. `src/components/ui/button.tsx` exists and renders correctly
6. `src/lib/utils.ts` exports `cn` function
</verification>

<success_criteria>
- The project builds cleanly with `npm run build` (zero errors)
- TypeScript strict mode is enforced
- TailwindCSS 4 utilities work (verified by rendered page)
- shadcn/ui components render with correct styling
- All base dependencies installed (next, react, typescript, tailwindcss, shadcn components, clsx, tailwind-merge)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
