---
phase: 06-compliance-and-legal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/CookieConsent.tsx
  - src/lib/gtm.ts
  - src/app/[locale]/layout.tsx
  - src/components/sections/contact/GoogleMap.tsx
  - src/styles/globals.css
autonomous: true
requirements:
  - CMPL-01
  - CMPL-02

must_haves:
  truths:
    - "On first visit, a cookie consent banner appears with granular toggles for analytics, marketing, and functional cookies"
    - "User's consent choice persists across sessions (banner does not reappear after accepting)"
    - "When consent is not granted, no GA4/GTM events fire (no gtag network requests before banner interaction)"
    - "Google Maps iframe only loads after functionality consent is granted"
  artifacts:
    - path: "src/components/layout/CookieConsent.tsx"
      provides: "Client component wrapping vanilla-cookieconsent with categories, i18n, and GTM consent callbacks"
      min_lines: 40
    - path: "src/lib/gtm.ts"
      provides: "GTM Consent Mode v2 helpers (updateGtagConsent, default consent script content)"
      exports: ["updateGtagConsent", "GTM_CONSENT_DEFAULT_SCRIPT"]
    - path: "src/components/sections/contact/GoogleMap.tsx"
      provides: "Google Maps with consent gate wired to cookie consent state"
      contains: "acceptedCategory"
  key_links:
    - from: "src/components/layout/CookieConsent.tsx"
      to: "src/lib/gtm.ts"
      via: "imports updateGtagConsent for consent callbacks"
      pattern: "updateGtagConsent"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/layout/CookieConsent.tsx"
      via: "CookieConsentBanner mounted in body"
      pattern: "CookieConsentBanner"
    - from: "src/app/[locale]/layout.tsx"
      to: "GTM scripts"
      via: "next/script with consent default before GTM container"
      pattern: "gtm-consent-default"
---

<objective>
Install vanilla-cookieconsent v3 and build the GDPR cookie consent system with granular category toggles (analytics, marketing, functional), persistent cookie storage, and GTM Consent Mode v2 gating. Wire the existing Google Maps consent gate placeholder to the cookie consent state.

Purpose: Legal GDPR compliance for EU visitors — no tracking without explicit consent, granular category control, and consent persistence across sessions.
Output: CookieConsentBanner component, GTM consent helpers, layout integration, Google Maps consent wiring.
</objective>

<execution_context>
@/Users/mihaigrigore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mihaigrigore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-compliance-and-legal/06-RESEARCH.md
@src/app/[locale]/layout.tsx
@src/components/sections/contact/GoogleMap.tsx
@src/styles/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vanilla-cookieconsent, create GTM helpers and CookieConsent component</name>
  <files>
    src/lib/gtm.ts
    src/components/layout/CookieConsent.tsx
    src/styles/globals.css
  </files>
  <action>
1. Install vanilla-cookieconsent: `npm install vanilla-cookieconsent`

2. Create `src/lib/gtm.ts` with:
   - `GTM_CONSENT_DEFAULT_SCRIPT` string constant containing the inline script that sets `gtag('consent', 'default', {...})` with all storage types denied and `wait_for_update: 500` (see RESEARCH.md Pattern 2)
   - `updateGtagConsent()` function that reads `CookieConsent.acceptedCategory('analytics')`, `CookieConsent.acceptedCategory('marketing')`, `CookieConsent.acceptedCategory('functionality')` and calls `window.gtag('consent', 'update', {...})` mapping analytics→analytics_storage, marketing→ad_storage+ad_user_data+ad_personalization, functionality→functionality_storage
   - Add `window.gtag` type augmentation for TypeScript
   - Guard with `typeof window === 'undefined'` check

3. Create `src/components/layout/CookieConsent.tsx` as a `'use client'` component:
   - Import `vanilla-cookieconsent/dist/cookieconsent.css` and `* as CookieConsent from 'vanilla-cookieconsent'`
   - Accept `locale: string` prop
   - In `useEffect`, call `CookieConsent.run({...})` with:
     - categories: necessary (enabled, readOnly), analytics (autoClear _ga/_gid cookies), marketing (autoClear _gcl/_fbp cookies), functionality
     - language: default=locale, translations for 'ro' and 'en' with full Romanian GDPR-compliant text (banner title, description, accept all, reject all, manage preferences, category names and descriptions)
     - onFirstConsent, onConsent, onChange callbacks all calling `updateGtagConsent()` from `@/lib/gtm`
     - guiOptions: set consentModal layout to 'box' and position to 'bottom right'
   - Return null (library manages its own DOM)

4. In `src/styles/globals.css`, add vanilla-cookieconsent CSS overrides to match project brand:
   - Override `--cc-bg`, `--cc-text`, `--cc-btn-primary-bg`, `--cc-btn-primary-text` etc. with Burgundy/Black/Grey/White palette values
   - Ensure banner sits above other content (z-index compatible with header)
  </action>
  <verify>
    <automated>cd /Users/mihaigrigore/workspace/AceAgencyWebsite-V2- && npm run build 2>&1 | tail -20</automated>
    <manual>Check that vanilla-cookieconsent is in package.json dependencies</manual>
  </verify>
  <done>CookieConsentBanner component created with 4 consent categories, bilingual RO/EN text, and GTM Consent Mode v2 update callbacks. GTM helper module exports updateGtagConsent and default consent script. Brand-matched CSS overrides in globals.css.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CookieConsent into layout and connect Google Maps consent gate</name>
  <files>
    src/app/[locale]/layout.tsx
    src/components/sections/contact/GoogleMap.tsx
  </files>
  <action>
1. In `src/app/[locale]/layout.tsx`:
   - Import `Script` from `next/script`
   - Import `CookieConsentBanner` from `@/components/layout/CookieConsent`
   - Import `GTM_CONSENT_DEFAULT_SCRIPT` from `@/lib/gtm`
   - Add inside `<head>` (or before body content): `<Script id="gtm-consent-default" strategy="beforeInteractive">{GTM_CONSENT_DEFAULT_SCRIPT}</Script>`
   - Conditionally add GTM container script: only if `process.env.NEXT_PUBLIC_GTM_ID` is set, add `<Script id="gtm-script" strategy="afterInteractive">` with the standard GTM snippet using the env var
   - Add `<CookieConsentBanner locale={locale} />` after `</SmoothScroll>` but before closing `</NextIntlClientProvider>` (inside body, after main content)

2. In `src/components/sections/contact/GoogleMap.tsx`:
   - Convert to `'use client'` component
   - Import `useState, useEffect` from React
   - Import `* as CookieConsent` from `vanilla-cookieconsent`
   - Replace `const consentGranted = true;` with:
     - `const [consentGranted, setConsentGranted] = useState(() => { try { return CookieConsent.acceptedCategory('functionality'); } catch { return false; } });`
     - `useEffect` listening for `cc:onChange` and `cc:onConsent` window events to update state via `setConsentGranted(CookieConsent.acceptedCategory('functionality'))`
   - Update the no-consent placeholder text to be bilingual (use useTranslations or hardcode both languages based on html lang attribute — prefer useTranslations from 'contact' namespace with a new key `map.consentRequired`)
   - Add the consent key to both ro.json and en.json under `contact.map.consentRequired`
  </action>
  <verify>
    <automated>cd /Users/mihaigrigore/workspace/AceAgencyWebsite-V2- && npm run build 2>&1 | tail -20</automated>
    <manual>Visit localhost:3000/ro/ — consent banner should appear on first visit. After accepting, Google Maps on /contact should load. After declining functionality, Google Maps should show consent placeholder.</manual>
  </verify>
  <done>CookieConsentBanner mounted in layout.tsx with GTM consent default script before GTM container. Google Maps iframe gated behind functionality consent with dynamic state updates on consent change. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. On first visit, cookie consent banner appears with category toggles
3. Accepting cookies persists choice (banner doesn't reappear on refresh)
4. Google Maps on /contact only loads after functionality consent granted
5. No GTM/GA4 network requests fire before consent interaction (verify in Network tab — note: GTM won't fire at all without NEXT_PUBLIC_GTM_ID env var, which is expected in dev)
</verification>

<success_criteria>
- Cookie consent banner renders with 4 categories (necessary, analytics, marketing, functional) in both RO and EN
- Consent persists across page loads via cookie
- GTM Consent Mode v2 default script executes before GTM container
- Google Maps consent gate is functional (no iframe without functionality consent)
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-compliance-and-legal/06-01-SUMMARY.md`
</output>
