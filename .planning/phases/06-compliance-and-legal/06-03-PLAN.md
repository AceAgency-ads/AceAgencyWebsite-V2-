---
phase: 06-compliance-and-legal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/intrebari-frecvente/page.tsx
  - src/components/sections/faq/FAQPageContent.tsx
  - src/messages/ro.json
  - src/messages/en.json
autonomous: true
requirements:
  - PAGE-11

must_haves:
  truths:
    - "FAQ page renders at /ro/intrebari-frecvente and /en/intrebari-frecvente with accordion-style questions and answers"
    - "FAQ page contains comprehensive agency-related questions (not service-specific — those live on service pages)"
    - "FAQPage JSON-LD schema markup is present in page source"
    - "FAQ page content is bilingual (RO and EN)"
  artifacts:
    - path: "src/app/[locale]/intrebari-frecvente/page.tsx"
      provides: "FAQ page route with generateMetadata and FAQPage schema"
      min_lines: 30
    - path: "src/components/sections/faq/FAQPageContent.tsx"
      provides: "FAQ accordion section for standalone FAQ page"
      min_lines: 30
  key_links:
    - from: "src/app/[locale]/intrebari-frecvente/page.tsx"
      to: "src/messages/*.json"
      via: "next-intl getTranslations namespace 'faq'"
      pattern: "getTranslations.*faq"
    - from: "src/components/sections/faq/FAQPageContent.tsx"
      to: "src/components/ui/accordion"
      via: "shadcn Accordion component"
      pattern: "Accordion"
---

<objective>
Build a standalone FAQ page at `/intrebari-frecvente` with comprehensive agency-related questions, shadcn Accordion UI, FAQPage JSON-LD schema, and full bilingual content.

Purpose: SEO-valuable FAQ page covering general agency questions (pricing, process, timelines, locations, etc.) that complements the service-specific FAQs already on individual service pages. Provides rich result eligibility via FAQPage schema.
Output: FAQ page route, FAQPageContent component, bilingual i18n content with 12-15 Q&A pairs.
</objective>

<execution_context>
@/Users/mihaigrigore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mihaigrigore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-compliance-and-legal/06-RESEARCH.md
@src/components/sections/contact/ContactFAQ.tsx
@src/app/[locale]/contact/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FAQ page route with FAQPageContent component and full bilingual content</name>
  <files>
    src/app/[locale]/intrebari-frecvente/page.tsx
    src/components/sections/faq/FAQPageContent.tsx
    src/messages/ro.json
    src/messages/en.json
  </files>
  <action>
1. Create `src/components/sections/faq/FAQPageContent.tsx`:
   - `'use client'` component (needs useTranslations)
   - Follow ContactFAQ.tsx pattern exactly: useTranslations('faq'), t.raw('items') as ReadonlyArray of {question, answer}
   - Group FAQ items into categories using t.raw('categories') as ReadonlyArray of { name: string, items: ReadonlyArray of { question, answer } }
   - For each category: render a SectionHeader with the category name, then an Accordion with the items
   - OR simpler approach: flat list like ContactFAQ but with more items and optional category headings via H3 elements between groups
   - Use SectionWrapper theme alternating (first category light, second dark, etc.) OR single light SectionWrapper with all items
   - Decision: Use a single SectionWrapper theme="light" with category H3 subheadings separating groups. This matches the clean legal page aesthetic.
   - Include FAQPage JSON-LD schema with ALL questions across all categories in a single mainEntity array
   - Style accordion items identically to ContactFAQ (text-lg font-semibold trigger, text-base muted content)

2. Create `src/app/[locale]/intrebari-frecvente/page.tsx`:
   - Async server page following contact/page.tsx pattern
   - generateMetadata with:
     - title: "Intrebari Frecvente - AceAgency" (RO) / "Frequently Asked Questions - AceAgency" (EN)
     - description: max 155 chars with CTA
     - canonical, hreflang alternates for ro/en/x-default
     - OG + Twitter Card tags
   - Import LegalHero from `@/components/sections/legal/LegalHero` (reuse from Plan 02)
     - If LegalHero is not yet available (parallel execution), create a simple hero inline: SectionWrapper theme="dark" with Breadcrumb + heading
     - Breadcrumb: Home > Intrebari Frecvente
   - Render FAQPageContent below the hero

3. Add `faq` namespace to `src/messages/ro.json` with 12-15 comprehensive Q&A pairs organized in 3-4 categories:
   - meta.title, meta.description, breadcrumb.home, breadcrumb.current, hero.heading, hero.overline
   - Category 1 "Despre AceAgency" (3-4 items): Ce este AceAgency?, Ce servicii oferiti?, In ce locatii operati?, Care este experienta echipei?
   - Category 2 "Proces si Colaborare" (3-4 items): Cum incepe o colaborare?, Cat dureaza un proiect?, Cum comunicam pe parcursul proiectului?, Oferiti raportare?
   - Category 3 "Preturi si Contracte" (3-4 items): Cat costa serviciile?, Exista contracte pe termen lung?, Ce metode de plata acceptati?, Oferiti garantii?
   - Category 4 "Suport si Contact" (2-3 items): Cum va pot contacta?, Cat de repede raspundeti?, Aveti program de suport?
   - Each answer: 2-4 sentences, professional tone, naturally mention relevant services and include internal linking text (e.g., "vizitati pagina noastra de servicii" or "completati formularul de contact")
   - Use proper Romanian diacritics (ă, â, î, ș, ț)
   - Structure: `{ categories: [{ name, items: [{ question, answer }] }] }` OR flat `{ items: [{ question, answer, category }] }`
   - Choose flat structure for simplicity: `{ items: [{ question, answer }] }` with items ordered by category

4. Add `faq` namespace to `src/messages/en.json` with equivalent English content:
   - Same structure, same number of items, translated
   - Natural English, not machine-translation feel
  </action>
  <verify>
    <automated>cd /Users/mihaigrigore/workspace/AceAgencyWebsite-V2- && npm run build 2>&1 | tail -20</automated>
    <manual>Visit /ro/intrebari-frecvente and /en/intrebari-frecvente — both should render FAQ accordion with 12-15 items. View page source to confirm FAQPage JSON-LD schema.</manual>
  </verify>
  <done>FAQ page renders at /intrebari-frecvente in both RO and EN with 12-15 Q&A pairs in accordion format. FAQPage JSON-LD schema present in page source. Content covers agency, process, pricing, and support topics. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. FAQ page accessible at /ro/intrebari-frecvente and /en/intrebari-frecvente
3. Accordion expands/collapses with keyboard navigation (Tab + Enter/Space)
4. FAQPage JSON-LD schema visible in page source with all Q&A pairs
5. Content is bilingual and comprehensive (12-15 items covering 3-4 categories)
6. Proper meta tags (title, description, canonical, hreflang, OG)
</verification>

<success_criteria>
- FAQ page renders correctly in both locales
- Accordion UI matches existing FAQ patterns (ContactFAQ, ServiceFAQ)
- FAQPage JSON-LD schema validates (structured as @type: FAQPage with mainEntity array)
- 12-15 comprehensive Q&A pairs covering general agency topics
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-compliance-and-legal/06-03-SUMMARY.md`
</output>
