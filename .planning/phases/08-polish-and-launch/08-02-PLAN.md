---
phase: 08-polish-and-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/app/[locale]/not-found.tsx
  - src/app/not-found.tsx
  - src/messages/ro.json
  - src/messages/en.json
autonomous: true
requirements:
  - CMPL-05
  - CMPL-06
  - CMPL-07

must_haves:
  truths:
    - "Security headers (HSTS, X-Content-Type-Options, X-Frame-Options, Referrer-Policy) are set on all responses"
    - "Visiting a non-existent URL shows a branded 404 page with navigation back to the site"
    - "The 404 page renders correctly in both RO and EN locales"
    - "HTTPS redirect and non-www redirect are handled by Vercel (no custom code needed)"
  artifacts:
    - path: "next.config.ts"
      provides: "Security headers configuration"
      contains: "Strict-Transport-Security"
    - path: "src/app/[locale]/not-found.tsx"
      provides: "Locale-aware 404 page with i18n"
      contains: "useTranslations"
    - path: "src/app/not-found.tsx"
      provides: "Global fallback 404 page for unknown paths"
      min_lines: 10
  key_links:
    - from: "next.config.ts"
      to: "all responses"
      via: "async headers() function"
      pattern: "async headers"
    - from: "src/app/[locale]/not-found.tsx"
      to: "src/messages/ro.json"
      via: "useTranslations('notFound')"
      pattern: "useTranslations.*notFound"
---

<objective>
Add security headers to all responses via next.config.ts, create branded bilingual 404 pages, and document Vercel HTTPS/redirect handling.

Purpose: Meet launch-blocking security (CMPL-05), redirect (CMPL-06), and error page (CMPL-07) requirements.
Output: Security headers on all routes, branded 404 page in both locales, Vercel redirect documentation.
</objective>

<execution_context>
@/Users/mihaigrigore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mihaigrigore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-polish-and-launch/08-RESEARCH.md
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add security headers to next.config.ts</name>
  <files>
    next.config.ts
  </files>
  <action>
Add an `async headers()` function to next.config.ts that returns security headers for all routes:

```typescript
const securityHeaders = [
  { key: 'Strict-Transport-Security', value: 'max-age=63072000; includeSubDomains; preload' },
  { key: 'X-Content-Type-Options', value: 'nosniff' },
  { key: 'X-Frame-Options', value: 'SAMEORIGIN' },
  { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
];
```

Apply to all routes via `source: '/(.*)'`. Keep existing turbopack.root config. Follow the exact pattern from 08-RESEARCH.md Pattern 1.

**Note on CMPL-06 (HTTPS/redirects):** Vercel automatically handles HTTP-to-HTTPS redirects and non-www redirects via domain settings. No code changes needed. Document this in the SUMMARY as "handled by Vercel infrastructure."

**Do NOT add CSP header** — it's complex with GTM, Cal.com, Google Maps, Analytics, and fonts. Deferred per research recommendation.
  </action>
  <verify>
    <automated>cd /Users/mihaigrigore/workspace/AceAgencyWebsite-V2- && npx tsc --noEmit 2>&1 | head -5 && grep -c "Strict-Transport-Security" next.config.ts</automated>
  </verify>
  <done>next.config.ts exports headers() with HSTS, X-Content-Type-Options, X-Frame-Options, and Referrer-Policy on all routes. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create branded 404 page with bilingual support</name>
  <files>
    src/app/[locale]/not-found.tsx
    src/app/not-found.tsx
    src/messages/ro.json
    src/messages/en.json
  </files>
  <action>
**1. Add i18n keys** to both `ro.json` and `en.json`:

```json
"notFound": {
  "title": "404",
  "heading": "Pagina nu a fost gasita" / "Page not found",
  "description": "Ne pare rau, pagina pe care o cauti nu exista sau a fost mutata." / "Sorry, the page you're looking for doesn't exist or has been moved.",
  "backHome": "Inapoi acasa" / "Back to home"
}
```

**2. Create `src/app/[locale]/not-found.tsx`** (locale-aware):
- Import `useTranslations` from `next-intl`
- Render a centered section with:
  - Large "404" heading using font-glacial and brand accent color (burgundy)
  - Description paragraph in grey
  - "Back to home" link styled as a button using brand accent background
- Use the project's design language (dark background section, brand typography)
- Import `Link` from `@/i18n/navigation` for locale-aware navigation

**3. Create `src/app/not-found.tsx`** (global fallback):
- For completely unknown URLs outside `[locale]` segment
- Hardcoded Romanian text (primary locale) since `useTranslations` is not available
- Include a link to `/ro/` (primary locale homepage)
- Render full HTML structure (this renders outside any layout)
- Keep it simple — minimal HTML with inline styles or Tailwind if available

**Caveat from research:** The `[locale]/not-found.tsx` only triggers when `notFound()` is called programmatically. The root `src/app/not-found.tsx` handles truly unknown URLs. Both are needed.
  </action>
  <verify>
    <automated>cd /Users/mihaigrigore/workspace/AceAgencyWebsite-V2- && npx tsc --noEmit 2>&1 | head -10 && test -f src/app/not-found.tsx && test -f "src/app/[locale]/not-found.tsx" && echo "Both 404 files exist"</automated>
  </verify>
  <done>Locale-aware 404 page renders with brand styling and navigation link. Global fallback 404 exists for unknown paths. Both ro.json and en.json have notFound namespace. TypeScript compiles.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes successfully
3. `next.config.ts` contains `Strict-Transport-Security`, `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`
4. Both `not-found.tsx` files exist and contain branded content
5. `ro.json` and `en.json` both contain `notFound` namespace keys
</verification>

<success_criteria>
Security headers are configured on all routes. Both locale-aware and global 404 pages exist with branded design and navigation. HTTPS and non-www redirects documented as Vercel-handled. Production build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-polish-and-launch/08-02-SUMMARY.md`
</output>
