---
phase: 08-polish-and-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/animations/ScrollReveal.tsx
  - src/components/animations/ScrubReveal.tsx
  - src/components/animations/TextReveal.tsx
  - src/components/layout/SmoothScroll.tsx
  - src/components/sections/services/ServicesGrid.tsx
  - src/components/sections/services/ServiceFeatures.tsx
  - src/components/sections/services/ServiceProcess.tsx
  - src/components/sections/services/WhyChooseSection.tsx
  - src/components/sections/services/ProcessSteps.tsx
autonomous: true
requirements:
  - SEO-06
  - CMPL-04

must_haves:
  truths:
    - "All animation components respect prefers-reduced-motion (no animation when user prefers reduced motion)"
    - "Lenis smooth scroll is disabled when prefers-reduced-motion is active"
    - "Elements animated by ScrollReveal are immediately visible (opacity 1, y 0) in reduced-motion mode"
    - "Production build completes with zero errors"
  artifacts:
    - path: "src/components/animations/ScrollReveal.tsx"
      provides: "Reduced-motion safe scroll reveal animation"
      contains: "matchMedia"
    - path: "src/components/layout/SmoothScroll.tsx"
      provides: "Reduced-motion bypass for Lenis smooth scroll"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "src/components/animations/ScrollReveal.tsx"
      to: "gsap.matchMedia"
      via: "matchMedia condition check"
      pattern: "prefers-reduced-motion"
    - from: "src/components/layout/SmoothScroll.tsx"
      to: "window.matchMedia"
      via: "early return when reduced motion"
      pattern: "prefers-reduced-motion.*reduce"
---

<objective>
Retrofit all animation components missing prefers-reduced-motion support and disable Lenis smooth scroll when reduced motion is preferred. This addresses both WCAG 2.1 AA accessibility (CMPL-04) and lays groundwork for performance (SEO-06) by ensuring no unnecessary animation overhead.

Purpose: Users who request reduced motion must see content immediately without animation. This is a WCAG 2.1 AA requirement and also reduces unnecessary GPU/CPU work.
Output: All 7 animation-related files updated with gsap.matchMedia reduced-motion handling.
</objective>

<execution_context>
@/Users/mihaigrigore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mihaigrigore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-polish-and-launch/08-RESEARCH.md

Key reference files for established pattern:
@src/components/animations/TextReveal.tsx (already has matchMedia — use as reference)
@src/components/sections/home/StatsSection.tsx (CountUp already has matchMedia — use as reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrofit ScrollReveal and SmoothScroll with reduced-motion handling</name>
  <files>
    src/components/animations/ScrollReveal.tsx
    src/components/layout/SmoothScroll.tsx
  </files>
  <action>
**ScrollReveal.tsx** — Wrap the existing `gsap.from()` animation inside `gsap.matchMedia()`:
- Add `const mm = gsap.matchMedia();`
- Move existing animation into `mm.add('(prefers-reduced-motion: no-preference)', () => { ... });`
- Add `mm.add('(prefers-reduced-motion: reduce)', () => { gsap.set(containerRef.current!, { opacity: 1, y: 0 }); });`
- Keep the ScrollTrigger.refresh() logic inside the no-preference branch only
- Follow the exact pattern from TextReveal.tsx (already has matchMedia)

**SmoothScroll.tsx** — Add early return when reduced motion is preferred:
- At the start of the useEffect (or useGSAP), check `window.matchMedia('(prefers-reduced-motion: reduce)').matches`
- If true, return early (skip Lenis initialization entirely — browser native scroll is used instead)
- This follows the pattern from 08-RESEARCH.md Pattern 4
  </action>
  <verify>
    <automated>cd /Users/mihaigrigore/workspace/AceAgencyWebsite-V2- && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify ScrollReveal.tsx and SmoothScroll.tsx contain 'prefers-reduced-motion' string</manual>
  </verify>
  <done>ScrollReveal wraps animation in matchMedia with no-preference and reduce branches. SmoothScroll skips Lenis when reduced motion is active. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Retrofit 5 service section components with reduced-motion handling</name>
  <files>
    src/components/sections/services/ServicesGrid.tsx
    src/components/sections/services/ServiceFeatures.tsx
    src/components/sections/services/ServiceProcess.tsx
    src/components/sections/services/WhyChooseSection.tsx
    src/components/sections/services/ProcessSteps.tsx
  </files>
  <action>
For each of the 5 service section components that use GSAP directly without reduced-motion guards (identified in 08-RESEARCH.md audit):

1. **Read each file** to identify the GSAP animation calls (gsap.from, gsap.to, gsap.fromTo, ScrollTrigger usage)
2. **Wrap animations** inside `gsap.matchMedia()`:
   - `mm.add('(prefers-reduced-motion: no-preference)', () => { /* existing animation code */ });`
   - `mm.add('(prefers-reduced-motion: reduce)', () => { gsap.set(elements, { opacity: 1, y: 0, x: 0 }); });` — set all animated elements to their final visible state immediately
3. **Follow the established pattern** from TextReveal.tsx and CountUp.tsx (already have matchMedia)
4. Keep existing useGSAP scope/dependency patterns unchanged — only wrap the inner animation logic

**Important:** In the reduce branch, use `gsap.set()` to explicitly make elements visible. Do NOT just skip the animation — elements may start with opacity: 0 from the animation's initial state.
  </action>
  <verify>
    <automated>cd /Users/mihaigrigore/workspace/AceAgencyWebsite-V2- && npx tsc --noEmit 2>&1 | head -20 && grep -l "prefers-reduced-motion" src/components/sections/services/ServicesGrid.tsx src/components/sections/services/ServiceFeatures.tsx src/components/sections/services/ServiceProcess.tsx src/components/sections/services/WhyChooseSection.tsx src/components/sections/services/ProcessSteps.tsx</automated>
  </verify>
  <done>All 5 service section components wrap GSAP animations in matchMedia with reduced-motion branches. Elements are immediately visible when user prefers reduced motion. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes successfully
3. All 7 modified files contain 'prefers-reduced-motion' string
4. No animation component uses `gsap.from`, `gsap.to`, or `gsap.fromTo` outside a matchMedia wrapper
</verification>

<success_criteria>
Every animation component in the codebase respects prefers-reduced-motion. Users with reduced motion see all content immediately with no animation. Lenis smooth scroll falls back to native browser scrolling. Production build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-polish-and-launch/08-01-SUMMARY.md`
</output>
