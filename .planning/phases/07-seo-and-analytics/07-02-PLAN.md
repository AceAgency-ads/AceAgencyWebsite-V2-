---
phase: 07-seo-and-analytics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seo/schemas.ts
  - src/app/[locale]/page.tsx
  - src/app/[locale]/despre-noi/page.tsx
  - src/app/[locale]/servicii/page.tsx
  - src/app/[locale]/servicii/[slug]/page.tsx
  - src/app/[locale]/contact/page.tsx
  - src/app/[locale]/intrebari-frecvente/page.tsx
  - src/app/[locale]/politica-confidentialitate/page.tsx
  - src/app/[locale]/politica-cookies/page.tsx
  - src/app/[locale]/termeni-si-conditii/page.tsx
autonomous: true
requirements:
  - SEO-01
  - SEO-05

must_haves:
  truths:
    - "Homepage has Organization + LocalBusiness JSON-LD visible in page source"
    - "Service sub-pages have Service + FAQ + BreadcrumbList JSON-LD in page source"
    - "About, Services index, Contact pages have Organization + BreadcrumbList JSON-LD"
    - "FAQ page has FAQPage + BreadcrumbList JSON-LD"
    - "Legal pages have BreadcrumbList JSON-LD only"
    - "All JSON-LD validates without errors in Google Rich Results Test"
    - "Breadcrumbs are present on all pages except homepage"
  artifacts:
    - path: "src/lib/seo/schemas.ts"
      provides: "JSON-LD builder functions for Organization, LocalBusiness, Service, FAQ, BreadcrumbList"
      exports: ["organizationSchema", "localBusinessSchema", "serviceSchema", "faqSchema", "breadcrumbSchema"]
  key_links:
    - from: "src/app/[locale]/page.tsx"
      to: "src/lib/seo/schemas.ts"
      via: "organizationSchema() and localBusinessSchema() calls rendered as <script type='application/ld+json'>"
      pattern: "application/ld\\+json"
    - from: "src/app/[locale]/servicii/[slug]/page.tsx"
      to: "src/lib/seo/schemas.ts"
      via: "serviceSchema() and faqSchema() calls"
      pattern: "serviceSchema|faqSchema"
---

<objective>
Create centralized JSON-LD schema builder functions and inject structured data into all pages via JSX `<script type="application/ld+json">` tags (the official Next.js pattern). Replace the broken `metadata.other` approach on existing pages. Verify breadcrumb coverage on all non-homepage pages.

Purpose: Enable rich results in Google Search (Organization knowledge panel, LocalBusiness, Service, FAQ rich snippets, breadcrumbs) and fix broken JSON-LD that currently renders as `<meta>` tags instead of `<script>` tags.
Output: `src/lib/seo/schemas.ts` with 5 builder functions, JSON-LD injected into all 9 non-thank-you pages.
</objective>

<execution_context>
@/Users/mihaigrigore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mihaigrigore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-seo-and-analytics/07-RESEARCH.md
@src/components/layout/Breadcrumb.tsx
@src/app/[locale]/servicii/[slug]/page.tsx
@src/app/[locale]/contact/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JSON-LD schema builder functions</name>
  <files>
    src/lib/seo/schemas.ts
  </files>
  <action>
Create `src/lib/seo/schemas.ts` with 5 pure builder functions. Import `SITE_URL` from `./constants.ts` (created by Plan 01, but if running in parallel, define SITE_URL inline as fallback constant).

**organizationSchema():**
Returns Organization with: name "AceAgency", url, logo (`${SITE_URL}/images/logo.png`), email "cretualin@aceagency.ro", telephone "+40750465757", address (PostalAddress: Bulevardul Aviatorilor 106, Bucuresti, RO), sameAs array (LinkedIn, Instagram, Facebook URLs — use placeholder URLs like `https://linkedin.com/company/aceagency` etc.).

**localBusinessSchema():**
Returns LocalBusiness with `@id: '${SITE_URL}/#localbusiness'`, extends Organization fields, adds: geo (GeoCoordinates: lat 44.4664, lng 26.0841 — Aviatorilor area), openingHoursSpecification (Mo-Fr 09:00-18:00), priceRange "$$".

**serviceSchema(params: { name: string; description: string; url: string }):**
Returns Service with provider (Organization ref), areaServed (Country: Romania), serviceType.

**faqSchema(items: readonly { question: string; answer: string }[]):**
Returns FAQPage with mainEntity array of Question/Answer pairs. Strip any HTML tags from answers using a simple regex `answer.replace(/<[^>]*>/g, '')`.

**breadcrumbSchema(items: readonly { name: string; url: string }[]):**
Returns BreadcrumbList with itemListElement array. Each item has position (1-indexed), name, and item (url).

Also export a helper `renderJsonLd(schema: Record<string, unknown>): string` that does `JSON.stringify(schema).replace(/</g, '\\u003c')` for XSS protection per Next.js docs.

All functions return plain objects (no schema-dts dependency). Use `as const` for literal types where appropriate.
  </action>
  <verify>
    <automated>cd /Users/mihaigrigore/workspace/AceAgencyWebsite-V2- && npx tsc --noEmit src/lib/seo/schemas.ts 2>&1 | head -20</automated>
  </verify>
  <done>schemas.ts exports 5 builder functions + renderJsonLd helper. All compile without errors. Each function returns a valid schema.org JSON-LD object.</done>
</task>

<task type="auto">
  <name>Task 2: Inject JSON-LD into all pages and verify breadcrumbs</name>
  <files>
    src/app/[locale]/page.tsx
    src/app/[locale]/despre-noi/page.tsx
    src/app/[locale]/servicii/page.tsx
    src/app/[locale]/servicii/[slug]/page.tsx
    src/app/[locale]/contact/page.tsx
    src/app/[locale]/intrebari-frecvente/page.tsx
    src/app/[locale]/politica-confidentialitate/page.tsx
    src/app/[locale]/politica-cookies/page.tsx
    src/app/[locale]/termeni-si-conditii/page.tsx
  </files>
  <action>
Add JSON-LD `<script type="application/ld+json">` tags to page JSX using `dangerouslySetInnerHTML={{ __html: renderJsonLd(schema) }}` pattern. Each page wraps content in a fragment `<>` and prepends the JSON-LD script(s).

**Per-page schema mapping (from RESEARCH.md audit):**

| Page | JSON-LD Schemas |
|------|----------------|
| Home (`page.tsx`) | Organization + LocalBusiness |
| About (`despre-noi`) | Organization + BreadcrumbList |
| Services index (`servicii`) | Organization + BreadcrumbList |
| Service sub-pages (`servicii/[slug]`) | Service + FAQ + BreadcrumbList |
| Contact | Organization + BreadcrumbList |
| FAQ (`intrebari-frecvente`) | FAQPage + BreadcrumbList |
| Privacy | BreadcrumbList |
| Cookies | BreadcrumbList |
| Terms | BreadcrumbList |

**For service sub-pages:**
- Use `serviceSchema({ name: t('title'), description: t('description'), url })` for Service schema.
- For FAQ schema: read FAQ items from i18n using `t.raw('{serviceKey}.faq.items')` and pass to `faqSchema()`.
- Build breadcrumb items from the existing breadcrumbItems array used by ServiceHero, converting to `{ name, url }` format for `breadcrumbSchema()`.

**For FAQ page:**
- Read all FAQ items from i18n categories and flatten into question/answer pairs for `faqSchema()`.
- The page already has FAQPage JSON-LD from Phase 6 — verify it exists and update to use the centralized builder.

**For pages with existing Breadcrumb component:**
The Breadcrumb component already renders BreadcrumbList JSON-LD via JSX `<script>`. Do NOT duplicate. For these pages, only add the non-breadcrumb schemas (Organization, Service, FAQ). Check if Breadcrumb component is used — if yes, skip breadcrumbSchema() for that page. If the page lacks a Breadcrumb component but needs BreadcrumbList schema, add it via the schemas.ts builder.

**Important:** If Plan 01 runs in parallel and has not yet removed `metadata.other` with `script:ld+json`, remove it here as well (safe to do from either plan).
  </action>
  <verify>
    <automated>cd /Users/mihaigrigore/workspace/AceAgencyWebsite-V2- && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>All 9 pages (excluding multumim) have JSON-LD rendered as `<script type="application/ld+json">` in their JSX. No page uses `metadata.other` for JSON-LD. Homepage has Organization + LocalBusiness. Service pages have Service + FAQ. FAQ page has FAQPage. All inner pages have BreadcrumbList (either via Breadcrumb component or schemas.ts builder). Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. `curl -s http://localhost:3000/ro/ | grep 'application/ld+json'` shows Organization and LocalBusiness schemas on homepage
3. `curl -s http://localhost:3000/ro/servicii/google-ads | grep 'application/ld+json'` shows Service schema
4. `curl -s http://localhost:3000/ro/intrebari-frecvente | grep 'FAQPage'` shows FAQPage schema
5. Copy JSON-LD from any page source and paste into Google Rich Results Test — validates without errors
6. Breadcrumb component present on all pages except homepage (verify via grepping page files for Breadcrumb import or breadcrumbSchema call)
</verification>

<success_criteria>
- JSON-LD rendered as `<script type="application/ld+json">` on all 9 indexed pages
- No `metadata.other` with `script:ld+json` anywhere in codebase
- Homepage: Organization + LocalBusiness
- Service sub-pages: Service + FAQ + BreadcrumbList
- Legal pages: BreadcrumbList only
- All schemas use centralized builder functions from schemas.ts
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-seo-and-analytics/07-02-SUMMARY.md`
</output>
