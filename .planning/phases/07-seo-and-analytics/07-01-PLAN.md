---
phase: 07-seo-and-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seo/constants.ts
  - src/lib/seo/metadata.ts
  - src/app/[locale]/page.tsx
  - src/app/[locale]/despre-noi/page.tsx
  - src/app/[locale]/servicii/page.tsx
  - src/app/[locale]/servicii/[slug]/page.tsx
  - src/app/[locale]/contact/page.tsx
  - src/app/[locale]/intrebari-frecvente/page.tsx
  - src/app/[locale]/politica-confidentialitate/page.tsx
  - src/app/[locale]/politica-cookies/page.tsx
  - src/app/[locale]/termeni-si-conditii/page.tsx
  - src/app/[locale]/multumim/page.tsx
  - src/messages/ro.json
  - src/messages/en.json
autonomous: true
requirements:
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-08

must_haves:
  truths:
    - "Every page has a <title> tag under 60 characters and a <meta name='description'> under 155 characters"
    - "Every page has <link rel='canonical'> and <link rel='alternate' hreflang> for ro, en, and x-default"
    - "Every page has Open Graph (og:title, og:description, og:url, og:site_name, og:locale) and Twitter Card tags"
    - "Home and About pages have generateMetadata with correct titles and descriptions from i18n"
    - "Each page has at least 3 internal links to other pages on the site"
  artifacts:
    - path: "src/lib/seo/constants.ts"
      provides: "SITE_URL, ORG_NAME, and other SEO constants"
    - path: "src/lib/seo/metadata.ts"
      provides: "generatePageMetadata() factory function"
      exports: ["generatePageMetadata"]
  key_links:
    - from: "src/app/[locale]/*/page.tsx"
      to: "src/lib/seo/metadata.ts"
      via: "generatePageMetadata() call in generateMetadata export"
      pattern: "generatePageMetadata"
    - from: "src/lib/seo/metadata.ts"
      to: "src/lib/seo/constants.ts"
      via: "SITE_URL import"
      pattern: "SITE_URL"
---

<objective>
Create a centralized metadata factory and apply complete SEO metadata (title, description, canonical, hreflang, OG, Twitter Card) to all 10 pages. Add missing meta i18n keys for Home and About. Audit and add internal links where needed (SEO-08: min 3-5 per page).

Purpose: Eliminate 40+ lines of metadata boilerplate per page, ensure consistent SEO across all pages, and fix the 2 pages (Home, About) that currently have no generateMetadata at all.
Output: `src/lib/seo/metadata.ts` factory, all 10 pages using it, meta i18n keys for home/about added.
</objective>

<execution_context>
@/Users/mihaigrigore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mihaigrigore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-seo-and-analytics/07-RESEARCH.md
@src/lib/seo/metadata.ts (to be created)
@src/app/[locale]/page.tsx
@src/app/[locale]/despre-noi/page.tsx
@src/app/[locale]/servicii/[slug]/page.tsx
@src/app/[locale]/contact/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SEO constants and metadata factory</name>
  <files>
    src/lib/seo/constants.ts
    src/lib/seo/metadata.ts
  </files>
  <action>
Create `src/lib/seo/` directory with two files:

**constants.ts:**
- `SITE_URL = 'https://aceagency.ro'` (extracted from hardcoded strings across all pages)
- `ORG_NAME = 'AceAgency'`
- `DEFAULT_OG_IMAGE = '/images/og-default.jpg'` (path for future OG image — set as default)

**metadata.ts:**
- Export `generatePageMetadata(params)` function that accepts `{ title, description, path, locale, noIndex?, ogImage? }` and returns a complete `Metadata` object.
- Include: `title`, `description`, `alternates.canonical` (`${SITE_URL}/${locale}${path}`), `alternates.languages` (ro, en, x-default), `openGraph` (title, description, url, siteName, locale mapped to ro_RO/en_US, type: 'website', image), `twitter` (card: 'summary_large_image', title, description).
- Handle `noIndex` by adding `robots: { index: false, follow: false }`.
- Use `SITE_URL` and `ORG_NAME` from constants.ts — no hardcoded URLs.
- Homepage path should be `''` (empty string), producing canonical `https://aceagency.ro/ro` (no trailing slash — consistent with Next.js behavior per RESEARCH.md Pitfall 4).

Follow the pattern from RESEARCH.md Pattern 1 exactly. Use `import type { Metadata } from 'next'`.
  </action>
  <verify>
    <automated>cd /Users/mihaigrigore/workspace/AceAgencyWebsite-V2- && npx tsc --noEmit src/lib/seo/metadata.ts src/lib/seo/constants.ts 2>&1 | head -20</automated>
  </verify>
  <done>Both files exist, export correct types, and compile without errors. generatePageMetadata returns a complete Metadata object with canonical, hreflang (ro, en, x-default), OG, and Twitter Card fields.</done>
</task>

<task type="auto">
  <name>Task 2: Apply metadata factory to all pages, add missing i18n meta keys, and audit internal links</name>
  <files>
    src/app/[locale]/page.tsx
    src/app/[locale]/despre-noi/page.tsx
    src/app/[locale]/servicii/page.tsx
    src/app/[locale]/servicii/[slug]/page.tsx
    src/app/[locale]/contact/page.tsx
    src/app/[locale]/intrebari-frecvente/page.tsx
    src/app/[locale]/politica-confidentialitate/page.tsx
    src/app/[locale]/politica-cookies/page.tsx
    src/app/[locale]/termeni-si-conditii/page.tsx
    src/app/[locale]/multumim/page.tsx
    src/messages/ro.json
    src/messages/en.json
  </files>
  <action>
**i18n meta keys (add FIRST):**
Add `meta.title` and `meta.description` keys to `home` and `about` namespaces in both `ro.json` and `en.json`:
- Home RO: title "Agentie Marketing Digital Bucuresti - AceAgency" (~50 chars), description "AceAgency - agentie de marketing digital din Bucuresti. Servicii Google Ads, Facebook Ads, SEO, Email Marketing. Contacteaza-ne!" (~130 chars with CTA)
- Home EN: title "Digital Marketing Agency Bucharest - AceAgency" (~47 chars), description "AceAgency - digital marketing agency from Bucharest. Google Ads, Facebook Ads, SEO, Email Marketing services. Contact us today!" (~126 chars with CTA)
- About RO: title "Despre Noi - Agentie Marketing Premium - AceAgency" (~52 chars), description "Descopera echipa AceAgency - agentie de marketing digital din Bucuresti. Misiunea, valorile si povestea noastra." (~112 chars)
- About EN: title "About Us - Premium Marketing Agency - AceAgency" (~48 chars), description "Discover the AceAgency team - digital marketing agency from Bucharest. Our mission, values, and story." (~103 chars)

**Apply factory to all pages:**

For **Home** (`page.tsx`): Add `generateMetadata` export using `generatePageMetadata({ title: t('meta.title'), description: t('meta.description'), path: '', locale })`.

For **About** (`despre-noi/page.tsx`): Add `generateMetadata` export using factory with path `/despre-noi`.

For **existing pages** (servicii, servicii/[slug], contact, intrebari-frecvente, 3 legal pages, multumim): Replace the hand-written metadata return with `generatePageMetadata()` call. **Remove** the `other: { 'script:ld+json': ... }` field from metadata — JSON-LD will be handled by Plan 02 via JSX script tags. Keep the page-specific logic for getting title/description from i18n translations.

For **multumim**: Use `noIndex: true` since it's a thank-you page that should not be indexed.

**Internal linking audit (SEO-08):**
Review each page's content sections and CTA links. Most pages already have 3+ internal links via navigation, CTAs, and cross-references. For any page under 3 internal links, add contextual links:
- Ensure homepage hero/CTA links to `/servicii` and `/contact`
- Ensure service pages cross-link to related services in FAQ answers (already done in Phase 4)
- Ensure About page CTA links to `/servicii` and `/contact`
- Ensure legal pages have link back to homepage and contact in their content
- Count is satisfied by: header nav links + footer links + in-page CTAs + breadcrumbs. Focus on ensuring CTAs have proper href attributes, not adding artificial links.
  </action>
  <verify>
    <automated>cd /Users/mihaigrigore/workspace/AceAgencyWebsite-V2- && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>All 10 pages use generatePageMetadata(). Home and About have generateMetadata exports. No page has hardcoded 'https://aceagency.ro' in its metadata (all via SITE_URL constant). `metadata.other` with `script:ld+json` is removed from servicii/[slug] and contact pages. Build succeeds with zero errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. `curl -s http://localhost:3000/ro/ | grep '<title>'` shows correct Romanian title under 60 chars
3. `curl -s http://localhost:3000/ro/ | grep 'hreflang'` shows ro, en, x-default alternate links
4. `curl -s http://localhost:3000/ro/ | grep 'og:title'` shows Open Graph title
5. `curl -s http://localhost:3000/ro/despre-noi | grep '<title>'` shows About page title
6. `curl -s http://localhost:3000/ro/multumim | grep 'noindex'` shows noindex for thank-you page
</verification>

<success_criteria>
- Every page (10 total) has generateMetadata using the centralized factory
- Title tags are under 60 characters, descriptions under 155 characters
- Canonical URLs, hreflang (ro, en, x-default), OG, and Twitter Card present on every page
- No hardcoded 'https://aceagency.ro' strings in page files (all via SITE_URL)
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-seo-and-analytics/07-01-SUMMARY.md`
</output>
