---
phase: 03-flagship-pages
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/[locale]/despre-noi/page.tsx
  - src/components/sections/about/AboutHero.tsx
  - src/components/sections/about/StorySection.tsx
  - src/components/sections/about/ValuesSection.tsx
  - src/components/sections/about/DivisionsSection.tsx
  - src/components/sections/about/MissionVision.tsx
  - src/components/sections/about/WhyChooseUs.tsx
  - src/messages/ro.json
  - src/messages/en.json
autonomous: true
requirements:
  - PAGE-02
  - DSGN-02
  - DSGN-03
  - DSGN-06

must_haves:
  truths:
    - "The About page renders at /despre-noi with agency story, values, divisions, mission/vision, and why-choose-us sections"
    - "The About page alternates dark/light sections matching the page spec theme sequence"
    - "Values and divisions cards have scroll-triggered stagger animations and hover interactions"
    - "The About hero uses word-level TextReveal animation"
    - "The About CTA section reuses CTASection from homepage with about namespace"
    - "All about content is bilingual in ro.json and en.json under the 'about' namespace"
  artifacts:
    - path: "src/app/[locale]/despre-noi/page.tsx"
      provides: "About page route with all sections composed"
      contains: "setRequestLocale"
    - path: "src/components/sections/about/AboutHero.tsx"
      provides: "About hero section with word-level TextReveal"
      contains: "TextReveal"
    - path: "src/components/sections/about/StorySection.tsx"
      provides: "Agency story with abstract geometry + timeline visual"
      contains: "story"
    - path: "src/components/sections/about/ValuesSection.tsx"
      provides: "6 value cards in 3-column grid"
      contains: "values"
    - path: "src/components/sections/about/DivisionsSection.tsx"
      provides: "4 division cards with hover effects"
      contains: "divisions"
    - path: "src/components/sections/about/MissionVision.tsx"
      provides: "Side-by-side mission and vision columns"
      contains: "mission"
    - path: "src/components/sections/about/WhyChooseUs.tsx"
      provides: "Numbered differentiator list with stagger animation"
      contains: "whyUs"
  key_links:
    - from: "src/app/[locale]/despre-noi/page.tsx"
      to: "src/components/sections/about/"
      via: "imports all about section components"
      pattern: "import.*(AboutHero|StorySection|ValuesSection)"
    - from: "src/app/[locale]/despre-noi/page.tsx"
      to: "src/components/sections/home/CTASection.tsx"
      via: "reuses CTASection with namespace='about'"
      pattern: "CTASection.*namespace"
    - from: "src/messages/ro.json"
      to: "src/components/sections/about/"
      via: "about.* namespace keys"
      pattern: "about"
---

<objective>
Build the complete About page (Despre Noi) with all 7 sections — hero, story, values, divisions, mission/vision, why-choose-us, and CTA — reusing components from Plan 01 and CTASection from Plan 02.

Purpose: The About page is the second flagship page that proves AceAgency's design capability. It shares the same visual language and animation patterns as the homepage but tells the agency's story. The CTA section is reused from the homepage.

Output: Complete About page at /despre-noi with all sections, full bilingual content, and matching animation quality.
</objective>

<execution_context>
@/Users/mihaigrigore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mihaigrigore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-flagship-pages/03-01-SUMMARY.md
@design-system/pages/despre-noi.md
@design-system/components.md
@src/messages/ro.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build About page sections (Hero, Story, Values, Divisions)</name>
  <files>
    src/components/sections/about/AboutHero.tsx
    src/components/sections/about/StorySection.tsx
    src/components/sections/about/ValuesSection.tsx
    src/components/sections/about/DivisionsSection.tsx
  </files>
  <action>
    **1. Create AboutHero (`src/components/sections/about/AboutHero.tsx`):**
    - 'use client' directive.
    - `useTranslations('about')`.
    - Wrapped in `SectionWrapper` with `theme="dark"`. NOT hero={true} — this is an inner page, use smaller padding: pass `className="py-24 md:py-32"` or use default section padding which is already appropriate.
    - Layout: Left-aligned content.
    - Overline: `t('hero.overline')` — "DESPRE NOI", 12px uppercase, letter-spacing 0.12em, text Grey.
    - Headline: `t('hero.headline')` — "Disciplina. Viziune. Rezultate." Use `TextReveal` with `variant="word"` and `trigger="load"` (or `trigger="scroll"` since it's below the fold on inner pages — use scroll trigger with `start: 'top 85%'`). Rendered as `<h1>`, `text-[2.25rem] md:text-[3rem] lg:text-[4rem]` (display-lg responsive sizes).
    - Subheading: `t('hero.subheading')` — Red Hat Display, text Grey, `text-lg md:text-xl`, fades up via ScrollReveal.
    - Background: Subtle brand glow positioned top-right (not center like homepage) — using a div with `background: var(--ds-gradient-brand-glow)`, absolutely positioned top-right.
    - No CTAs in hero — "scroll to explore" pattern per spec.

    **2. Create StorySection (`src/components/sections/about/StorySection.tsx`):**
    - 'use client' directive.
    - `useTranslations('about')`.
    - Wrapped in `SectionWrapper` with `theme="light"`.
    - Two-column layout: `flex flex-col lg:flex-row gap-8 lg:gap-12`.
    - Left column (60%: `lg:w-[60%]`):
      - `SectionHeader` with overline: `t('story.overline')`, heading: `t('story.heading')`.
      - 2-3 paragraphs from i18n: `t.raw('story.paragraphs')` — iterate and render as `<p>` elements with `text-lg text-[var(--section-text-muted)] max-w-[65ch]`, `space-y-6` gap between.
      - Paragraphs stagger fade up via GSAP (use useGSAP to animate `[data-animate="paragraph"]` children with stagger: 0.1).
    - Right column (40%: `lg:w-[40%]`):
      - **Per user decision (CONTEXT.md):** Combination of abstract geometry (floating brand-colored shapes) and timeline milestones. Claude's discretion on balance.
      - Implementation approach: Create a decorative component with:
        - 3-4 geometric shapes (circles, rectangles) using brand colors (Burgundy, Grey, Black) — `absolute` positioned, `rounded-full` or `rounded-lg`, varying sizes (80px to 200px), `opacity-20` to `opacity-40`.
        - 2-3 milestone labels overlaid: year + brief text (e.g., "2021 — Fondare", "2023 — Full-Service", "2025 — AI Division") — `text-xs font-medium`, positioned along a vertical line.
        - Vertical timeline line: 1px border, Grey at `opacity-30`.
        - GSAP animation: shapes float gently (`y: -10, yoyo, repeat: -1, duration: 3-5`). Timeline items stagger fade in on scroll.
      - Mobile: Hidden or simplified (show only timeline without shapes).
    - Overall section fades in from right for the visual column.

    **3. Create ValuesSection (`src/components/sections/about/ValuesSection.tsx`):**
    - 'use client' directive.
    - `useTranslations('about')`.
    - Wrapped in `SectionWrapper` with `theme="dark"`.
    - `SectionHeader` with overline: `t('values.overline')`, heading: `t('values.heading')`, align: 'center'.
    - 6 value cards in a `BentoGrid` with `columns={3}`.
    - Each card: `data-animate="card"`.
      - `bg-[var(--section-card-bg)]`, `rounded-xl`, `p-6`, `border border-[var(--section-border)]`.
      - Icon: From lucide-react (Target, Shield, Zap, Users, TrendingUp, Heart — per spec). Size 32px, `text-[var(--section-text-muted)]`.
      - Title: `<h4>`, `text-lg font-bold`.
      - Description: `text-sm text-[var(--section-text-muted)]`.
    - Icon mapping: Define a static array mapping icon names from i18n to lucide-react components. Read value items from `t.raw('values.items')`.
    - Hover: `hover:translate-y-[-4px] hover:border-[var(--section-accent)] transition-all duration-300` (DSGN-06).

    **4. Create DivisionsSection (`src/components/sections/about/DivisionsSection.tsx`):**
    - 'use client' directive.
    - `useTranslations('about')`.
    - Wrapped in `SectionWrapper` with `theme="dark"`. Use slightly different background shade — add `className="bg-[var(--ds-color-dark-elevated)]"` to differentiate from adjacent dark sections.
    - `SectionHeader` with overline: `t('divisions.overline')`, heading: `t('divisions.heading')`, description: `t('divisions.description')`.
    - 4 division cards in `BentoGrid` with `columns={4}`.
    - Each card: Feature Card variant.
      - `data-animate="card"`.
      - `bg-[var(--section-card-bg)]`, `rounded-xl`, `p-6 md:p-8`, `border border-[var(--section-border)]`.
      - Icon: From lucide-react (Code, Target, Brain, Camera — per spec). Size 48px, `text-[var(--section-text-muted)]`.
      - Division name: `<h3>`, `text-xl font-bold`.
      - Focus area: `text-sm text-burgundy font-medium`.
      - Description: `text-sm text-[var(--section-text-muted)]`.
    - Hover (DSGN-06): `hover:translate-y-[-4px] hover:shadow-[var(--ds-shadow-burgundy)] transition-all duration-300`.
    - Each card links to the respective services sub-page (AceWeb → `/servicii`, AceAds → `/servicii/google-ads`, etc.). Use Link from `@/i18n/navigation`.
    - Read division items from `t.raw('divisions.items')`.
  </action>
  <verify>
    - `npm run build` completes without errors.
    - All 4 component files exist with valid TypeScript.
    - `grep "data-theme" src/components/sections/about/AboutHero.tsx` — uses SectionWrapper with theme.
    - Values section renders 6 cards, Divisions section renders 4 cards.
    - Hover effects work on both Values and Divisions cards.
  </verify>
  <done>
    AboutHero renders with word-level TextReveal and brand glow. StorySection shows 2-3 paragraphs with abstract geometry + timeline visual on the right. ValuesSection displays 6 value cards in a 3-column bento grid with hover effects. DivisionsSection shows 4 division cards with feature card styling and links to service pages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build remaining About sections, compose page, and add all bilingual content</name>
  <files>
    src/components/sections/about/MissionVision.tsx
    src/components/sections/about/WhyChooseUs.tsx
    src/app/[locale]/despre-noi/page.tsx
    src/messages/ro.json
    src/messages/en.json
  </files>
  <action>
    **1. Create MissionVision (`src/components/sections/about/MissionVision.tsx`):**
    - 'use client' directive.
    - `useTranslations('about')`.
    - Wrapped in `SectionWrapper` with `theme="light"`.
    - Two-column layout: `grid grid-cols-1 lg:grid-cols-2 gap-8`.
    - Each column is a card-like container:
      - `bg-[var(--section-card-bg)]`, `rounded-xl`, `p-8`, light-muted background.
      - Overline: `text-burgundy` (different from standard muted — accent color per spec).
      - Heading: `<h3>`, `text-2xl font-bold`.
      - Description: `text-lg text-[var(--section-text-muted)]`.
    - Left column: Mission (`t('mission.overline')`, `t('mission.heading')`, `t('mission.description')`).
    - Right column: Vision (`t('vision.overline')`, `t('vision.heading')`, `t('vision.description')`).
    - Desktop: Vertical 1px divider between columns — use `lg:border-r border-[var(--section-border)]` on left column or a separator element.
    - Animation: Left column fades up first (ScrollReveal), right follows with 200ms delay (ScrollReveal with GSAP delay or separate ScrollReveal with slightly later start position).

    **2. Create WhyChooseUs (`src/components/sections/about/WhyChooseUs.tsx`):**
    - 'use client' directive.
    - `useTranslations('about')`.
    - Wrapped in `SectionWrapper` with `theme="dark"`.
    - `SectionHeader` with overline: `t('whyUs.overline')`, heading: `t('whyUs.heading')`.
    - 5 differentiator items in a vertical list (NOT cards — list items per spec).
    - Each item:
      - `py-6 border-b border-[var(--section-border)]` for separation.
      - Layout: Horizontal on desktop, vertical on mobile.
      - Number: `text-5xl md:text-6xl font-bold text-grey opacity-30` — decorative large number (01-05). Left-aligned as visual accent.
      - Title: `<h4>`, `text-lg font-bold text-white`.
      - Description: `text-[var(--section-text-muted)]`.
    - Animation: Items stagger fade up from left using GSAP (useGSAP with `gsap.from('[data-animate="item"]', { x: -30, opacity: 0, stagger: 0.1, ... })`), ScrollTrigger at 85%.
    - Read items from `t.raw('whyUs.items')`.

    **3. Compose About page (`src/app/[locale]/despre-noi/page.tsx`):**
    - Create the route directory and page file.
    - Async `AboutPage` component with `params: Promise<{ locale: string }>`.
    - `setRequestLocale(locale)` as first statement (per project pattern).
    - Inner `AboutPageContent` component (sync, uses hooks) renders:
      1. `<AboutHero />`
      2. `<StorySection />`
      3. `<ValuesSection />`
      4. `<DivisionsSection />`
      5. `<MissionVision />`
      6. `<WhyChooseUs />`
      7. `<CTASection namespace="about" />` — reuse from `src/components/sections/home/CTASection.tsx` with about namespace.
    - Wrap in `<main>`.

    **4. Add all bilingual content to ro.json and en.json:**
    - Add the complete `about` namespace with all keys from the About page spec's "Content Keys" section.
    - Romanian content:
      - `about.hero.overline`: "DESPRE NOI"
      - `about.hero.headline`: "Disciplina. Viziune. Rezultate."
      - `about.hero.subheading`: "Suntem o echipa care crede ca fiecare brand merita o prezenta digitala de exceptie."
      - `about.story.overline`: "POVESTEA NOASTRA"
      - `about.story.heading`: "De la Pasiune la Performanta"
      - `about.story.paragraphs`: Array of 3 paragraphs (origin story, growth journey, present day — use content from spec or create Romanian content matching the agency's voice)
      - `about.values.overline`: "VALORILE NOASTRE"
      - `about.values.heading`: "Principiile Care Ne Ghideaza"
      - `about.values.items`: Array of 6 objects with `icon`, `title`, `description` per spec
      - `about.divisions.overline`: "DIVIZIILE NOASTRE"
      - `about.divisions.heading`: "Expertiza Sub Un Singur Acoperis"
      - `about.divisions.description`: From spec
      - `about.divisions.items`: Array of 4 objects with `name`, `focus`, `description` per spec
      - `about.mission.overline`: "MISIUNEA NOASTRA"
      - `about.mission.heading`: "Rezultate Digitale Masurabile"
      - `about.mission.description`: From spec
      - `about.vision.overline`: "VIZIUNEA NOASTRA"
      - `about.vision.heading`: "Lider in Digital din Romania"
      - `about.vision.description`: From spec
      - `about.whyUs.overline`: "DE CE SA NE ALEGI"
      - `about.whyUs.heading`: "Avantajul AceAgency"
      - `about.whyUs.items`: Array of 5 objects with `number`, `title`, `description` per spec
      - `about.cta.heading`: "Hai sa Construim Impreuna"
      - `about.cta.description`: "Esti gata sa transformi prezenta digitala a brandului tau? Echipa noastra te asteapta."
      - `about.cta.primary`: "Contacteaza-ne"
      - `about.cta.secondary`: "Vezi Serviciile"
    - English content: Full English translations for all about.* keys.
    - Verify no key collisions with existing namespaces (header, navigation, footer, social, home).
  </action>
  <verify>
    - `npm run build` completes without errors.
    - `npm run dev` — visiting `/ro/despre-noi` renders all 7 sections.
    - Visiting `/en/despre-noi` renders all sections in English.
    - No missing translation key warnings in console.
    - Section theme sequence: dark → light → dark → dark(elevated) → light → dark → burgundy.
    - WhyChooseUs items have stagger animation from left.
    - MissionVision shows side-by-side columns with divider on desktop.
    - CTASection reuses correctly with about namespace (different heading/buttons than homepage).
  </verify>
  <done>
    Complete About page renders at /despre-noi with all 7 sections: Hero (dark, TextReveal), Story (light, geometry + timeline), Values (dark, 6 cards), Divisions (dark-elevated, 4 cards), Mission/Vision (light, side-by-side), Why Choose Us (dark, numbered list), CTA (burgundy, reused component). Full bilingual content in ro.json and en.json under about.* namespace. All animations fire once on scroll, hover interactions on cards, sections alternate dark/light with hard cuts.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` — zero errors
2. About page accessible at /ro/despre-noi and /en/despre-noi
3. About hero uses word-level TextReveal
4. Story section shows abstract geometry + timeline on right column
5. Values section: 6 cards in 3-column grid with hover effects
6. Divisions section: 4 cards linking to service pages
7. Mission/Vision: side-by-side with vertical divider on desktop
8. Why Choose Us: numbered list with stagger animation
9. CTA section reuses homepage CTASection with about namespace
10. All content bilingual, no missing translation warnings
11. Mobile responsive: sections stack, cards single column
</verification>

<success_criteria>
- Complete About page with 7 sections matching homepage visual quality
- CTASection successfully reused across pages via namespace prop
- Full bilingual content for about.* namespace in ro.json and en.json
- All animations consistent with homepage patterns (fire once, reduced motion support)
- Build passes, TypeScript strict mode, no i18n warnings
</success_criteria>

<output>
After completion, create `.planning/phases/03-flagship-pages/03-03-SUMMARY.md`
</output>
