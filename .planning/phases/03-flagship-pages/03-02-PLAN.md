---
phase: 03-flagship-pages
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/sections/home/StatsSection.tsx
  - src/components/sections/home/AboutPreview.tsx
  - src/components/sections/home/Testimonials.tsx
  - src/components/sections/home/CTASection.tsx
  - src/components/sections/home/Newsletter.tsx
  - src/app/[locale]/page.tsx
autonomous: true
requirements:
  - PAGE-01
  - DSGN-07
  - DSGN-09

must_haves:
  truths:
    - "Stats section shows animated count-up numbers (150+, 50+, 98%, 5 ani) triggered on scroll"
    - "Testimonials display in a bento grid with at least 5 testimonials visible without a carousel"
    - "The burgundy CTA section renders with heading TextReveal and dual action buttons"
    - "The newsletter section has an email form with GDPR checkbox (UI only, no server action)"
    - "The About Preview shows the AceAgency logo on the right column per user decision"
  artifacts:
    - path: "src/components/sections/home/StatsSection.tsx"
      provides: "Stats grid with CountUp animations"
      contains: "CountUp"
    - path: "src/components/sections/home/Testimonials.tsx"
      provides: "Bento grid testimonial layout"
      contains: "BentoGrid"
    - path: "src/components/sections/home/CTASection.tsx"
      provides: "Reusable burgundy CTA section"
      contains: "burgundy"
    - path: "src/components/sections/home/AboutPreview.tsx"
      provides: "About preview with AceAgency logo visual"
      contains: "ace-agency-logo"
    - path: "src/components/sections/home/Newsletter.tsx"
      provides: "Newsletter signup form UI"
      contains: "newsletter"
  key_links:
    - from: "src/components/sections/home/StatsSection.tsx"
      to: "src/components/animations/CountUp.tsx"
      via: "CountUp component for animated numbers"
      pattern: "import.*CountUp"
    - from: "src/components/sections/home/Testimonials.tsx"
      to: "src/components/sections/BentoGrid.tsx"
      via: "BentoGrid for testimonial card layout"
      pattern: "import.*BentoGrid"
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/sections/home/"
      via: "imports all 5 new section components"
      pattern: "import.*(StatsSection|AboutPreview|Testimonials|CTASection|Newsletter)"
---

<objective>
Complete the Homepage by building Stats, About Preview, Testimonials, CTA, and Newsletter sections — all with scroll-triggered animations.

Purpose: Deliver the full homepage experience with all 7 sections. Stats prove track record with animated counters, testimonials build trust via bento grid layout, CTA drives conversions, newsletter captures leads. The About Preview teases the About page and showcases the AceAgency logo.

Output: Complete homepage with all 7 sections rendering correctly in both RO and EN.
</objective>

<execution_context>
@/Users/mihaigrigore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mihaigrigore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-flagship-pages/03-01-SUMMARY.md
@design-system/pages/home.md
@design-system/components.md
@src/messages/ro.json
@public/ace-agency-logo.webp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Stats, About Preview, and Testimonials sections</name>
  <files>
    src/components/sections/home/StatsSection.tsx
    src/components/sections/home/AboutPreview.tsx
    src/components/sections/home/Testimonials.tsx
  </files>
  <action>
    **1. Create StatsSection (`src/components/sections/home/StatsSection.tsx`):**
    - 'use client' directive.
    - `useTranslations('home')`.
    - Wrapped in `SectionWrapper` with `theme="light"` (DSGN-03: alternating dark/light).
    - `SectionHeader` with overline: `t('stats.overline')`, heading: `t('stats.heading')`, align: 'center'.
    - 4-column grid: `grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8`.
    - Each stat card:
      - `CountUp` component with `end={number}`, `suffix={suffix}`, `className="text-4xl md:text-5xl lg:text-6xl font-bold text-burgundy"`.
      - Label below: `text-sm text-[var(--section-text-muted)]`, centered.
    - Stats data from i18n: `t('stats.items')` — define 4 stats (150+, 50+, 98%, 5 ani) as per page spec.
    - Read stats items from JSON: use `t.raw('stats.items')` to get the array, then iterate. Each item has `value` (number), `suffix` (string), `label` (string).
    - Optional: Vertical 1px dividers between stats on desktop using `border-r border-[var(--section-border)]` on all but last stat.

    **2. Create AboutPreview (`src/components/sections/home/AboutPreview.tsx`):**
    - 'use client' directive.
    - `useTranslations('home')`.
    - Wrapped in `SectionWrapper` with `theme="dark"`.
    - Two-column layout: `flex flex-col lg:flex-row gap-8 lg:gap-12 items-center`.
    - Left column (55% on desktop: `lg:w-[55%]`):
      - `SectionHeader` with overline: `t('about.overline')`, heading: `t('about.heading')`, description: `t('about.description')`.
      - CTA button: `t('about.cta')` — Link to `/despre-noi`, secondary/outline variant with ArrowRight icon.
      - Text fades up via ScrollReveal.
    - Right column (45%: `lg:w-[45%]`):
      - **Per user decision (CONTEXT.md):** Use the actual AceAgency logo file `public/ace-agency-logo.webp`.
      - Render via `next/image` with explicit width/height (read the image at implementation to determine dimensions — use reasonable defaults like 400x400).
      - Add subtle GSAP animation: float/pulse effect using `gsap.to(el, { y: -10, repeat: -1, yoyo: true, duration: 3, ease: 'power1.inOut' })` wrapped in matchMedia for reduced motion.
      - Add a brand glow behind the logo: `absolute` div with `bg-[var(--ds-gradient-brand-glow)]`, `w-[300px] h-[300px]`, `opacity-20`, centered behind logo.
    - Mobile: Stacked, text first, logo below.

    **3. Create Testimonials (`src/components/sections/home/Testimonials.tsx`):**
    - 'use client' directive.
    - `useTranslations('home')`.
    - Wrapped in `SectionWrapper` with `theme="dark"`.
    - `SectionHeader` with overline: `t('testimonials.overline')`, heading: `t('testimonials.heading')`, align: 'center'.
    - Testimonial cards in `BentoGrid` with `columns={3}`.
    - Each card has `data-animate="card"` attribute for BentoGrid stagger.
    - Card design (Testimonial Card from components.md):
      - `bg-[var(--section-card-bg)]`, `rounded-xl`, `p-6 md:p-8`, `border border-[var(--section-border)]`.
      - Quote: `text-[var(--section-text)]`, `text-sm md:text-base`, italic.
      - Star rating: 5 yellow/gold stars (use Star icon from lucide-react, filled via `fill="currentColor"`), `text-yellow-500` or `text-amber-400`.
      - Author name: `text-sm font-semibold text-[var(--section-text)]`.
      - Company: `text-xs text-[var(--section-text-muted)]`.
    - DSGN-09: Bento grid — at least 5 testimonials visible. Use 6 testimonials from page spec. Apply varied sizes: first 2 cards get `sm:col-span-1 lg:row-span-2` for taller display, remaining are standard size. This creates visual variety without a carousel.
    - Hover micro-interaction (DSGN-06): `hover:translate-y-[-4px] hover:border-[var(--section-accent)] transition-all duration-300`.
    - Read testimonial data from i18n: `t.raw('testimonials.items')`.
  </action>
  <verify>
    - `npm run build` completes without errors.
    - Stats section shows 4 animated counters on scroll.
    - About Preview shows the AceAgency logo on the right column.
    - Testimonials shows 6 cards in bento layout with varied sizes.
    - All content renders correctly in both /ro/ and /en/.
  </verify>
  <done>
    Stats section renders 4 animated count-up numbers in a responsive grid on a light background. About Preview shows agency intro text with CTA and the actual AceAgency logo (animated float) on the right. Testimonials shows 6 cards in a bento grid with star ratings, hover interactions, and stagger animation. All use data-theme section theming.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build CTA and Newsletter sections, wire all sections into homepage</name>
  <files>
    src/components/sections/home/CTASection.tsx
    src/components/sections/home/Newsletter.tsx
    src/app/[locale]/page.tsx
  </files>
  <action>
    **1. Create CTASection (`src/components/sections/home/CTASection.tsx`):**
    - 'use client' directive.
    - Props: `namespace?` (string, default 'home') — enables reuse for About page CTA with different i18n namespace.
    - `useTranslations(namespace)`.
    - Wrapped in `SectionWrapper` with `theme="burgundy"`.
    - Centered content layout: `text-center max-w-2xl mx-auto`.
    - Heading: `t('cta.heading')` — Use `TextReveal` with `variant="word"` for kinetic typography on scroll (default trigger is scroll). Rendered as `<h2>`, `text-3xl md:text-4xl lg:text-5xl font-bold text-white`.
    - Description: `t('cta.description')` — `text-lg text-white/90`, fades up via ScrollReveal.
    - Two CTAs:
      - Primary: `t('cta.primary')` — Grey bg, Black text (accent variant). Link to `/contact`.
      - Secondary: `t('cta.secondary')` — Ghost variant (white border, transparent bg). Link to `/portofoliu` — but since portfolio is V2 (not built yet), use `aria-disabled="true"` and `pointer-events-none opacity-50` to show it as disabled. Or link to `/servicii` instead. Per spec: "or disabled if V2" — disable it.
    - Buttons fade up via ScrollReveal after heading.
    - CTA section has `py-24 md:py-32` padding (override SectionWrapper default).

    **2. Create Newsletter (`src/components/sections/home/Newsletter.tsx`):**
    - 'use client' directive.
    - `useTranslations('home')`.
    - Wrapped in `SectionWrapper` with `theme="dark"`.
    - Centered content: `text-center max-w-xl mx-auto`.
    - `SectionHeader` with overline: `t('newsletter.overline')`, heading: `t('newsletter.heading')`, description: `t('newsletter.description')`, align: 'center'.
    - Form (extracted pattern — similar to Footer newsletter but standalone):
      - `max-w-md mx-auto`.
      - Email input + submit button in a row (same styling as Footer newsletter form).
      - GDPR checkbox required.
      - `onSubmit`: `e.preventDefault()` — Phase 5 wiring, UI only.
      - Form labels and placeholders from i18n: `t('newsletter.placeholder')`, `t('newsletter.submit')`.
    - Note: Do NOT extract a shared NewsletterForm component yet — Footer has its own form and this is a standalone section. Extraction can happen in Phase 5 when server actions are wired.
    - Fade up via ScrollReveal.

    **3. Wire all sections into homepage (`src/app/[locale]/page.tsx`):**
    - Update `HomePageContent` to import and render all 7 sections in order:
      1. `<HeroSection />` (from Plan 01)
      2. `<ServicesPreview />` (from Plan 01)
      3. `<StatsSection />`
      4. `<AboutPreview />`
      5. `<Testimonials />`
      6. `<CTASection />`
      7. `<Newsletter />`
    - Remove any remaining placeholder code or unused imports.
    - The `HomePageContent` component should be minimal — just importing and composing sections.
    - Keep the async `HomePage` wrapper with `setRequestLocale(locale)`.
  </action>
  <verify>
    - `npm run build` completes without errors.
    - Full homepage scrolls through all 7 sections: Hero (dark) → Services (dark) → Stats (light) → About Preview (dark) → Testimonials (dark) → CTA (burgundy) → Newsletter (dark).
    - Section backgrounds alternate correctly (dark → dark → light → dark → dark → burgundy → dark).
    - Stats counters animate on scroll.
    - CTA heading animates with TextReveal.
    - Newsletter form prevents default on submit.
    - Mobile PageSpeed: target 85+ (check with Lighthouse).
    - No missing translation key warnings in console.
  </verify>
  <done>
    Complete homepage with all 7 sections rendering correctly. CTA section uses burgundy theme with TextReveal heading and dual action buttons (Contact enabled, Portfolio disabled/V2). Newsletter section has email form with GDPR checkbox (UI only). All sections wire into page.tsx in correct order with alternating section themes.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` — zero errors
2. Full homepage scrolls through 7 sections with correct theme alternation
3. Stats counters animate from 0 to target values on scroll
4. Testimonials show 6 cards in bento grid (no carousel)
5. About Preview shows AceAgency logo on right column
6. CTA section has burgundy background with TextReveal heading
7. Newsletter form has GDPR checkbox, prevents default on submit
8. All content in RO and EN with no missing keys
9. Mobile responsive: sections stack properly on 375px viewport
</verification>

<success_criteria>
- All 7 homepage sections render in correct order with correct themes
- CountUp animations fire on scroll entry at 85% viewport
- Testimonial bento grid shows 5+ testimonials simultaneously
- CTA is reusable (accepts namespace prop for About page)
- Newsletter form is UI-ready for Phase 5 server action wiring
- Build passes, no TypeScript errors, no i18n warnings
</success_criteria>

<output>
After completion, create `.planning/phases/03-flagship-pages/03-02-SUMMARY.md`
</output>
