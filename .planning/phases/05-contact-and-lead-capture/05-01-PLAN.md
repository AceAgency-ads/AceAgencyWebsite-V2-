---
phase: 05-contact-and-lead-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .env.example
  - src/lib/resend.ts
  - src/lib/validations/contact-schema.ts
  - src/lib/validations/newsletter-schema.ts
  - src/lib/actions/contact.ts
  - src/lib/actions/newsletter.ts
  - src/app/[locale]/contact/page.tsx
  - src/app/[locale]/multumim/page.tsx
  - src/components/sections/contact/ContactHero.tsx
  - src/components/sections/contact/ContactForm.tsx
  - src/components/sections/contact/ContactInfo.tsx
  - src/components/sections/contact/GoogleMap.tsx
  - src/components/sections/contact/ContactFAQ.tsx
  - src/messages/ro.json
  - src/messages/en.json
autonomous: true
requirements:
  - PAGE-10
  - FUNC-01
  - FUNC-06

user_setup:
  - service: resend
    why: "Email delivery for contact form and newsletter"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard (resend.com) -> API Keys -> Create API Key"
    dashboard_config:
      - task: "Verify aceagency.ro domain (add DKIM/SPF DNS records)"
        location: "Resend Dashboard -> Domains -> Add Domain"

must_haves:
  truths:
    - "Contact page renders at /ro/contact and /en/contact with hero, form, info, map, and FAQ sections"
    - "Submitting contact form with valid data calls Resend and returns success state"
    - "Submitting contact form with invalid data shows field-level validation errors"
    - "Google Maps embed shows AceAgency office location with dark styling"
    - "Thank-you page renders at /ro/multumim and /en/multumim"
    - "FAQ accordion on contact page expands and collapses"
  artifacts:
    - path: "src/lib/resend.ts"
      provides: "Resend client singleton"
      contains: "new Resend"
    - path: "src/lib/actions/contact.ts"
      provides: "Contact form Server Action"
      exports: ["submitContactForm"]
    - path: "src/lib/actions/newsletter.ts"
      provides: "Newsletter Server Action"
      exports: ["submitNewsletter"]
    - path: "src/lib/validations/contact-schema.ts"
      provides: "Zod contact form schema"
      exports: ["contactFormSchema"]
    - path: "src/components/sections/contact/ContactForm.tsx"
      provides: "Contact form with useActionState"
      contains: "useActionState"
    - path: "src/app/[locale]/contact/page.tsx"
      provides: "Contact page route"
      contains: "ContactHero"
  key_links:
    - from: "src/components/sections/contact/ContactForm.tsx"
      to: "src/lib/actions/contact.ts"
      via: "useActionState(submitContactForm)"
      pattern: "useActionState.*submitContactForm"
    - from: "src/lib/actions/contact.ts"
      to: "src/lib/resend.ts"
      via: "resend.emails.send"
      pattern: "resend\\.emails\\.send"
    - from: "src/lib/actions/contact.ts"
      to: "src/lib/validations/contact-schema.ts"
      via: "Zod safeParse"
      pattern: "contactFormSchema\\.safeParse"
---

<objective>
Build the Contact page with all sections (Hero, Contact Form, Contact Info, Google Maps, FAQ), Server Actions for contact form and newsletter with Zod validation and Resend email delivery, and the thank-you page.

Purpose: This is the primary lead-capture page — visitors can submit inquiries and see agency contact details. The form, validation, and email delivery infrastructure also serves the newsletter (wired in Plan 02).
Output: Functional contact page with 5 sections, working contact form Server Action, Google Maps embed, and thank-you redirect page.
</objective>

<execution_context>
@/Users/mihaigrigore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mihaigrigore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system/pages/contact.md
@.planning/phases/05-contact-and-lead-capture/05-RESEARCH.md
@.planning/phases/05-contact-and-lead-capture/05-CONTEXT.md
@src/components/sections/SectionWrapper.tsx
@src/components/sections/SectionHeader.tsx
@src/components/sections/services/ServiceFAQ.tsx
@src/messages/ro.json
@src/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create Resend client, Zod schemas, Server Actions, and thank-you page</name>
  <files>
    package.json
    .env.example
    src/lib/resend.ts
    src/lib/validations/contact-schema.ts
    src/lib/validations/newsletter-schema.ts
    src/lib/actions/contact.ts
    src/lib/actions/newsletter.ts
    src/app/[locale]/multumim/page.tsx
  </files>
  <action>
    1. Install dependencies: `npm install resend zod @calcom/embed-react`
       - Zod is new to the project (not yet in package.json)
       - @calcom/embed-react installed now but used in Plan 02
       - If @calcom/embed-react fails due to peer deps, install with `--legacy-peer-deps` and note for Plan 02

    2. Create `.env.example` with:
       ```
       RESEND_API_KEY=re_xxxxxxxxxxxxx
       CAL_LINK=aceagency/consultatie
       ```

    3. Create `src/lib/resend.ts` — Resend client singleton:
       - Import Resend from 'resend'
       - Validate RESEND_API_KEY env var at module load (throw if missing in production, warn in development)
       - Export singleton instance
       - In development without key, export a mock that logs instead of sending

    4. Create `src/lib/validations/contact-schema.ts`:
       - Zod schema matching contact.md spec: name (min 2, max 100), email (valid email), phone (optional, Romanian pattern /^(\+40|0)[0-9]{9}$/ or empty string), service (enum of 8 options, optional), message (min 10, max 2000), honeypot (max 0 chars)
       - Use i18n-ready error message keys (not hardcoded strings): 'nameMinLength', 'emailInvalid', 'messageMinLength', etc.
       - Export schema and inferred type ContactFormData

    5. Create `src/lib/validations/newsletter-schema.ts`:
       - Zod schema: email (valid email), honeypot (max 0 chars)
       - Export schema and inferred type NewsletterFormData

    6. Create `src/lib/actions/contact.ts`:
       - 'use server' directive
       - Export ContactFormState type: { success: boolean; errors?: Record<string, string[]>; message?: string }
       - Export async function submitContactForm(prevState: ContactFormState, formData: FormData): Promise<ContactFormState>
       - Check honeypot first — if filled, return { success: true } (silent success to fool bots)
       - Parse formData into object, run contactFormSchema.safeParse
       - On validation failure: return { success: false, errors: result.error.flatten().fieldErrors }
       - On success: send email via resend.emails.send with:
         - from: 'AceAgency Contact <noreply@aceagency.ro>' (use 'onboarding@resend.dev' as fallback in dev)
         - to: ['cretualin@aceagency.ro']
         - replyTo: validated email
         - subject: `Nou mesaj de contact - ${name}`
         - html: formatted email body with all form fields
       - On Resend error: return { success: false, message: 'emailSendFailed' }
       - On success: return { success: true }

    7. Create `src/lib/actions/newsletter.ts`:
       - 'use server' directive
       - Export NewsletterState type: { success: boolean; error?: string }
       - Export async function submitNewsletter(prevState: NewsletterState, formData: FormData): Promise<NewsletterState>
       - Check honeypot — silent success if filled
       - Validate with newsletterSchema.safeParse
       - Send confirmation/capture email via Resend:
         - to: ['cretualin@aceagency.ro'] (notify agency of new subscriber)
         - subject: `Newsletter: ${email}`
       - Return success/failure state

    8. Create `src/app/[locale]/multumim/page.tsx`:
       - Server component with setRequestLocale
       - generateMetadata with i18n title/description
       - Simple thank-you page: SectionWrapper (dark theme), centered content with checkmark icon (Lucide CheckCircle), heading "Multumim!" / "Thank You!", description about 24h response time
       - Link back to homepage
       - Add i18n keys for thankYou namespace in both ro.json and en.json (done in Task 2 alongside other i18n)
  </action>
  <verify>
    <automated>cd /Users/mihaigrigore/workspace/AceAgencyWebsite-V2- && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Check that resend, zod, @calcom/embed-react appear in package.json dependencies</manual>
  </verify>
  <done>
    - resend, zod, @calcom/embed-react installed in package.json
    - Resend client singleton exports from lib/resend.ts
    - Contact and newsletter Zod schemas export typed schemas
    - Contact Server Action validates, checks honeypot, sends via Resend
    - Newsletter Server Action validates and sends via Resend
    - Thank-you page renders at /[locale]/multumim
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Contact page sections, route, and full bilingual i18n content</name>
  <files>
    src/app/[locale]/contact/page.tsx
    src/components/sections/contact/ContactHero.tsx
    src/components/sections/contact/ContactForm.tsx
    src/components/sections/contact/ContactInfo.tsx
    src/components/sections/contact/GoogleMap.tsx
    src/components/sections/contact/ContactFAQ.tsx
    src/messages/ro.json
    src/messages/en.json
  </files>
  <action>
    1. Add full bilingual i18n content to ro.json and en.json under "contact" namespace:
       - All keys from design-system/pages/contact.md Content Keys section
       - Hero: overline, headline, subheading
       - Form: all field labels, placeholders, submit button text, success title/description, error generic message, all validation message keys (nameMinLength, nameMaxLength, emailInvalid, messageMinLength, messageMaxLength, phoneInvalid)
       - Form service options array: Web, Google Ads, Facebook Ads, TikTok Ads, SEO, Email Marketing, Consultanta, Altele
       - Info: address (label + value "Bulevardul Aviatorilor 106, Bucuresti"), email (label + value "cretualin@aceagency.ro"), phone (label + value "0750 465 757"), hours (label + value "Luni - Vineri, 09:00 - 18:00")
       - Map: cookieRequired message
       - Booking: overline, heading, description (placeholder — content used in Plan 02)
       - FAQ: overline, heading, 5 FAQ items (question + answer) from contact.md spec
       - Also add "thankYou" namespace: heading, description, backHome button text
       - EN translations for all of the above

    2. Create `src/components/sections/contact/ContactHero.tsx`:
       - Server component (no 'use client')
       - Uses SectionWrapper (theme="dark") + Breadcrumb (Acasa > Contact)
       - Overline text (Grey, uppercase tracking), headline with TextReveal (variant="word"), subheading with fade-up
       - Left-aligned layout, py-24 desktop / py-16 mobile per contact.md spec
       - Uses useTranslations('contact') — wait, this is server component. Use getTranslations if async, or make it a client component for TextReveal animation. Since TextReveal requires client-side GSAP, make this 'use client' with useTranslations.

    3. Create `src/components/sections/contact/ContactForm.tsx`:
       - 'use client' directive
       - Uses useActionState(submitContactForm, initialState) from react
       - Uses useFormStatus for pending state in submit button (create inner SubmitButton component)
       - On-blur validation: for each field, run contactFormSchema.pick({fieldName: true}).safeParse() on blur, store client-side errors in local state
       - Fields: name (Input), email (Input type=email), phone (Input type=tel), service (Select — use native <select> styled with Tailwind for simplicity), message (Textarea)
       - Honeypot: hidden input with aria-hidden, tabIndex=-1, autocomplete="off", position absolute off-screen
       - Server-side errors from state.errors displayed below respective fields (red text)
       - Client-side on-blur errors also displayed below fields
       - On state.success: redirect to /multumim using router.push from @/i18n/navigation (useEffect watching state.success)
       - Loading state: button shows spinner (Lucide Loader2 animate-spin), form fields disabled
       - Styling: dark theme inputs (bg-white/5 border border-white/10 text-white placeholder:text-white/40), focus ring in burgundy
       - Animation: fields stagger fade-up on scroll (use ScrollReveal or inline GSAP)
       - i18n: useTranslations('contact') for all labels, placeholders, errors, button text

    4. Create `src/components/sections/contact/ContactInfo.tsx`:
       - 'use client' for ScrollReveal animation
       - Glass card variant (bg-white/5 backdrop-blur-sm border border-white/10 rounded-2xl p-8)
       - Items: MapPin (address), Mail (email as mailto: link), Phone (phone as tel: link), Clock (hours)
       - Each item: Lucide icon (24px, text-gray-400) + label (text-sm text-gray-400) + value (text-white)
       - Social links section at bottom with configurable data (use placeholder hrefs, same pattern as Footer)
       - Animation: card fades in from right, delayed 200ms
       - i18n: useTranslations('contact')

    5. Create `src/components/sections/contact/GoogleMap.tsx`:
       - Server component (no client JS needed for iframe)
       - Google Maps iframe embed for "Bulevardul Aviatorilor 106, Bucharest"
       - Generate proper embed URL from Google Maps
       - Dark styling via CSS filter: className="invert grayscale-[0.8] contrast-[1.1] hue-rotate-[200deg] saturate-[0.3]" (per RESEARCH.md)
       - Height: 400px desktop, 300px mobile
       - loading="lazy", referrerPolicy="no-referrer-when-downgrade"
       - Wrap in SectionWrapper (theme="dark"), full-width (no Container — map bleeds edge to edge)
       - Build with placeholder mechanism for cookie consent gate (Phase 6): conditionally render map or placeholder div with message. For now, always show map (consent check wired in Phase 6).
       - title attribute for accessibility

    6. Create `src/components/sections/contact/ContactFAQ.tsx`:
       - 'use client' for accordion interaction
       - Uses shadcn Accordion (same pattern as ServiceFAQ from Phase 4)
       - Max-width 800px centered
       - 5 FAQ items from i18n contact.faq.items array
       - Stagger fade-up animation on scroll
       - SectionWrapper (theme="dark") + SectionHeader (overline + heading)
       - i18n: useTranslations('contact')

    7. Create `src/app/[locale]/contact/page.tsx`:
       - Async server component with setRequestLocale
       - generateMetadata: title "Contact | AceAgency - Agentie Digitala Bucuresti" (per contact.md SEO), meta description, alternates for hreflang (placeholder for Phase 7)
       - Render sections in order: ContactHero, ContactFormSection (wrapping ContactForm + ContactInfo in 2-column SectionWrapper), GoogleMap, ContactFAQ
       - Note: BookingSection is a placeholder div — implemented in Plan 02
       - Two-column layout for form section: form 60% left, info 40% right on lg+, stacked on mobile
       - The page itself is a server component; form/info sections are client components imported into it
  </action>
  <verify>
    <automated>cd /Users/mihaigrigore/workspace/AceAgencyWebsite-V2- && npm run build 2>&1 | tail -20</automated>
    <manual>Visit http://localhost:3000/ro/contact — verify hero, form with validation, contact info card, dark Google Maps, and FAQ accordion all render correctly</manual>
  </verify>
  <done>
    - Contact page renders at /ro/contact and /en/contact with 5 sections
    - Contact form shows on-blur validation errors for invalid fields
    - Contact form submits via Server Action (success redirects to /multumim)
    - Contact info card shows address, email (mailto:), phone (tel:), hours, social placeholders
    - Google Maps shows dark-themed Bucharest office location
    - FAQ accordion expands/collapses with 5 items
    - All text is bilingual (RO + EN) via i18n
    - npm run build passes with zero errors
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors — contact page and multumim page render as static
2. TypeScript compiles without errors (`npx tsc --noEmit`)
3. Contact page at /ro/contact shows: hero with animated headline, 2-column form + info, Google Maps, FAQ accordion
4. Form validation: leave name empty and blur → error appears; enter invalid email and blur → error appears
5. Form submission with valid data → redirects to /multumim (email delivery requires RESEND_API_KEY)
6. FAQ accordion items expand and collapse
7. Google Maps iframe loads with dark styling
8. All content available in both /ro/ and /en/ locales
</verification>

<success_criteria>
- Contact page is the fully functional lead-capture page with form, info, map, and FAQ
- Server Actions for both contact and newsletter are ready (newsletter wired to UI in Plan 02)
- Thank-you page exists as conversion tracking destination
- All bilingual content is complete
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-contact-and-lead-capture/05-01-SUMMARY.md`
</output>
