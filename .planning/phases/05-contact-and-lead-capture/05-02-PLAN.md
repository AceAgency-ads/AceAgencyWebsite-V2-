---
phase: 05-contact-and-lead-capture
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/sections/contact/BookingSection.tsx
  - src/components/layout/Footer.tsx
  - src/app/[locale]/contact/page.tsx
  - src/messages/ro.json
  - src/messages/en.json
autonomous: false
requirements:
  - FUNC-02
  - FUNC-03

must_haves:
  truths:
    - "Cal.com booking widget renders inline on the contact page and allows selecting a meeting slot"
    - "Newsletter form in Footer submits via Server Action and shows success/error state"
    - "Newsletter form on contact page submits via Server Action and shows success/error state"
  artifacts:
    - path: "src/components/sections/contact/BookingSection.tsx"
      provides: "Cal.com inline embed with dark theme"
      contains: "Cal"
    - path: "src/components/layout/Footer.tsx"
      provides: "Newsletter form wired to Server Action"
      contains: "useActionState"
  key_links:
    - from: "src/components/sections/contact/BookingSection.tsx"
      to: "@calcom/embed-react"
      via: "Cal component import"
      pattern: "import Cal from.*@calcom/embed-react"
    - from: "src/components/layout/Footer.tsx"
      to: "src/lib/actions/newsletter.ts"
      via: "useActionState(submitNewsletter)"
      pattern: "useActionState.*submitNewsletter"
---

<objective>
Add Cal.com booking embed to the contact page and wire the Newsletter Server Action into the Footer and contact page newsletter section.

Purpose: Complete the two remaining lead-capture mechanisms — meeting booking via Cal.com and newsletter subscription with email capture via Resend. After this plan, all Phase 5 functionality is live.
Output: Cal.com booking section on contact page, Newsletter form in Footer wired to server action, visual verification of all lead-capture flows.
</objective>

<execution_context>
@/Users/mihaigrigore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mihaigrigore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-contact-and-lead-capture/05-CONTEXT.md
@.planning/phases/05-contact-and-lead-capture/05-RESEARCH.md
@.planning/phases/05-contact-and-lead-capture/05-01-SUMMARY.md
@src/components/layout/Footer.tsx
@src/app/[locale]/contact/page.tsx
@src/lib/actions/newsletter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Cal.com BookingSection and wire Newsletter Server Action into Footer</name>
  <files>
    src/components/sections/contact/BookingSection.tsx
    src/components/layout/Footer.tsx
    src/app/[locale]/contact/page.tsx
    src/messages/ro.json
    src/messages/en.json
  </files>
  <action>
    1. Create `src/components/sections/contact/BookingSection.tsx`:
       - 'use client' directive (Cal.com embed requires client-side JS)
       - Import Cal from '@calcom/embed-react'
       - If @calcom/embed-react import fails at build time (peer dep issue), implement fallback:
         - Fallback: use inline script embed via next/script with Cal.com's embed.js
         - Document the fallback approach in code comments
       - SectionWrapper with theme="light" (per contact.md Section 4)
       - Centered content: overline "PROGRAMEAZA", heading "Rezerva o Consultatie Gratuita", description
       - Cal component with:
         - calLink: use env var `process.env.NEXT_PUBLIC_CAL_LINK` or fallback to "aceagency/consultatie" (configurable)
         - style: width 100%, height 100%, overflow auto
         - config: { theme: "dark", hideEventTypeDetails: false }
       - Wrap Cal component in a container with:
         - max-w-[800px] mx-auto
         - min-h-[500px] (prevents layout jump while loading)
         - bg-[#262523] (dark background to prevent white flash during Cal.com load — per RESEARCH.md Pitfall 3)
         - rounded-2xl overflow-hidden
       - Loading skeleton: show a subtle pulsing skeleton while Cal.com embed loads
       - Animation: section fades up on scroll (ScrollReveal wrapper)
       - i18n: useTranslations('contact') for overline, heading, description

    2. Wire Newsletter Server Action into Footer:
       - Read current Footer.tsx to understand existing newsletter form structure
       - The Footer newsletter is currently UI-only with e.preventDefault() (per Phase 2/3 decisions)
       - Convert Footer to use 'use client' (if not already) for useActionState
       - Import submitNewsletter from '@/lib/actions/newsletter'
       - Replace the e.preventDefault() handler with useActionState:
         - const [state, formAction, pending] = useActionState(submitNewsletter, { success: false })
         - Wire form action={formAction}
         - Add honeypot hidden field (same pattern as contact form)
         - Show inline success message when state.success (e.g., "Multumim pentru abonare!" / "Thanks for subscribing!")
         - Show inline error message when state.error
         - Disable input + button while pending
         - KEEP the GDPR checkbox that already exists in the Footer newsletter form
       - Ensure Footer newsletter works on ALL pages (it's a global component)
       - Add newsletter success/error i18n keys to ro.json and en.json under "newsletter" namespace if not already present

    3. Update contact page to include BookingSection:
       - Add BookingSection import to contact/page.tsx
       - Insert BookingSection between GoogleMap and ContactFAQ sections (per contact.md page structure: Hero, Form+Info, Map, Booking, FAQ)
       - Ensure proper section ordering matches design spec

    4. Add NEXT_PUBLIC_CAL_LINK to .env.example:
       - Add: `NEXT_PUBLIC_CAL_LINK=aceagency/consultatie`
       - This is a public env var (NEXT_PUBLIC_ prefix) safe for client-side
  </action>
  <verify>
    <automated>cd /Users/mihaigrigore/workspace/AceAgencyWebsite-V2- && npm run build 2>&1 | tail -20</automated>
    <manual>
      1. Visit /ro/contact — verify BookingSection renders with heading and Cal.com embed (or loading skeleton if no Cal account configured)
      2. Visit any page — scroll to Footer newsletter form, enter email, submit — verify success message appears inline
      3. Check /en/contact — verify all booking and newsletter text is in English
    </manual>
  </verify>
  <done>
    - BookingSection renders on contact page with Cal.com embed (dark theme, centered, max 800px)
    - Cal.com embed loads inside dark background container (no white flash)
    - Footer newsletter form submits via Server Action on all pages
    - Newsletter success/error states display inline in Footer
    - GDPR checkbox preserved in Footer newsletter
    - Build passes with zero errors
    - All text bilingual (RO + EN)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of all lead-capture flows</name>
  <what-built>
    Complete Contact page with 5 sections (Hero, Contact Form + Info, Google Maps, Cal.com Booking, FAQ) and Newsletter Server Action wired into the global Footer. All lead-capture mechanisms are functional.
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:3000/ro/contact
       - Hero section: animated headline "Hai sa Vorbim despre Proiectul Tau"
       - Contact form: 5 fields (name, email, phone, service select, message) + submit button
       - Try submitting empty form → should show validation errors
       - Fill in valid data and submit → should redirect to /ro/multumim (email delivery requires RESEND_API_KEY in .env)
       - Contact info card: address, email (clickable mailto:), phone (clickable tel:), hours, social placeholders
       - Google Maps: dark-themed map showing Bulevardul Aviatorilor 106, Bucharest
       - Cal.com booking: section visible with embed (or placeholder if CAL_LINK not set)
       - FAQ: 5 accordion items that expand/collapse

    2. Visit http://localhost:3000/en/contact
       - All content is in English
       - Same functionality as Romanian version

    3. Scroll to Footer on any page
       - Newsletter form: enter email → submit → see inline success message
       - GDPR checkbox is present

    4. Visit http://localhost:3000/ro/multumim
       - Thank-you page with checkmark, heading, and link back to homepage

    5. Mobile view (resize to 375px width)
       - Form and info stack vertically
       - Map is 300px height
       - All sections are readable and tappable
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. Contact page renders all 5 sections in correct order (Hero, Form+Info, Map, Booking, FAQ)
3. Cal.com booking embed loads with dark theme (or graceful fallback)
4. Footer newsletter form is functional on all pages
5. All content bilingual (RO + EN)
6. Mobile responsive — form stacks, map resizes, all tap targets 48x48px
7. User visual approval of all lead-capture flows
</verification>

<success_criteria>
- All 5 Phase 5 success criteria from ROADMAP are met:
  1. Contact form with valid data sends email and shows success
  2. Contact form with invalid data shows field-level errors
  3. Cal.com booking widget renders and allows slot selection
  4. Google Maps embed shows office location
  5. Newsletter form captures email via Resend
- User has visually approved all flows
</success_criteria>

<output>
After completion, create `.planning/phases/05-contact-and-lead-capture/05-02-SUMMARY.md`
</output>
