---
phase: 04-service-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services.ts
  - src/components/ui/accordion.tsx
  - src/app/[locale]/servicii/page.tsx
  - src/app/[locale]/servicii/[slug]/page.tsx
  - src/components/sections/Breadcrumb.tsx
  - src/components/sections/services/ServicesHero.tsx
  - src/components/sections/services/ServicesGrid.tsx
  - src/components/sections/services/ProcessSteps.tsx
  - src/components/sections/services/WhyChooseSection.tsx
  - src/components/sections/services/ServiceHero.tsx
  - src/components/sections/services/ServiceFeatures.tsx
  - src/components/sections/services/ServiceProcess.tsx
  - src/components/sections/services/ServiceStats.tsx
  - src/components/sections/services/ServiceFAQ.tsx
  - src/components/sections/services/ServiceCTA.tsx
  - src/messages/ro.json
  - src/messages/en.json
autonomous: true
requirements:
  - PAGE-03
  - PAGE-04

must_haves:
  truths:
    - "The Services index page at /servicii renders with hero, 7-card services grid, process steps, why-choose-us, and CTA sections in both RO and EN"
    - "Each service card on the index page links to its respective sub-page URL"
    - "The Google Ads sub-page at /servicii/google-ads renders with ServiceHero, ServiceFeatures, ServiceProcess, ServiceStats, ServiceFAQ (accordion), and ServiceCTA"
    - "FAQ accordion items expand and collapse with keyboard navigation (Enter/Space)"
    - "Breadcrumbs render on both index and sub-pages with BreadcrumbList JSON-LD schema"
    - "All service pages generate statically via generateStaticParams (no dynamic rendering)"
  artifacts:
    - path: "src/lib/services.ts"
      provides: "Service registry with slug, i18nKey, icon mapping for all 6 services"
      exports: ["SERVICE_DEFINITIONS", "getServiceBySlug"]
    - path: "src/app/[locale]/servicii/page.tsx"
      provides: "Services index page with generateMetadata"
    - path: "src/app/[locale]/servicii/[slug]/page.tsx"
      provides: "Dynamic service sub-page with generateStaticParams and generateMetadata"
    - path: "src/components/sections/services/ServiceFAQ.tsx"
      provides: "FAQ accordion with FAQPage JSON-LD schema"
    - path: "src/components/sections/Breadcrumb.tsx"
      provides: "Reusable breadcrumb with BreadcrumbList JSON-LD"
  key_links:
    - from: "src/app/[locale]/servicii/[slug]/page.tsx"
      to: "src/lib/services.ts"
      via: "getServiceBySlug lookup + generateStaticParams"
      pattern: "getServiceBySlug\\(slug\\)"
    - from: "src/components/sections/services/ServiceFAQ.tsx"
      to: "src/messages/ro.json"
      via: "useTranslations('services') + t.raw() for FAQ items"
      pattern: "t\\.raw\\("
    - from: "src/components/sections/services/ServicesGrid.tsx"
      to: "src/app/[locale]/servicii/[slug]/page.tsx"
      via: "Link href to /servicii/{slug}"
      pattern: "Link.*href.*servicii"
---

<objective>
Build the Services index page and all 6 reusable Service section components, plus the dynamic sub-page route with Google Ads as the first fully wired sub-page.

Purpose: Establishes the complete service page infrastructure — service registry, shared section components, dynamic routing, breadcrumbs, FAQ accordion — so Plans 02 and 03 only need to add i18n content for remaining services.

Output: Functional /servicii index page + /servicii/google-ads sub-page with all animations, SEO metadata, schema markup, and bilingual content.
</objective>

<execution_context>
@/Users/mihaigrigore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mihaigrigore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-service-pages/04-RESEARCH.md
@design-system/pages/servicii.md
@src/components/sections/about/AboutHero.tsx
@src/components/sections/home/CTASection.tsx
@src/components/sections/SectionWrapper.tsx
@src/components/sections/SectionHeader.tsx
@src/components/animations/ScrollReveal.tsx
@src/components/animations/TextReveal.tsx
@src/components/animations/CountUp.tsx
@src/components/ui/SpotlightCard.tsx
@src/app/[locale]/despre-noi/page.tsx
@src/messages/ro.json
@src/messages/en.json
@Specificatii-Tehnice-SEO-AceAgency.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service registry, shadcn Accordion install, Breadcrumb component, and Services index page with all 5 sections</name>
  <files>
    src/lib/services.ts
    src/components/ui/accordion.tsx
    src/components/sections/Breadcrumb.tsx
    src/components/sections/services/ServicesHero.tsx
    src/components/sections/services/ServicesGrid.tsx
    src/components/sections/services/ProcessSteps.tsx
    src/components/sections/services/WhyChooseSection.tsx
    src/app/[locale]/servicii/page.tsx
    src/messages/ro.json
    src/messages/en.json
  </files>
  <action>
    **Step 1: Install shadcn Accordion**
    Run `npx shadcn@latest add accordion` to get `src/components/ui/accordion.tsx` with Radix primitives.

    **Step 2: Create `src/lib/services.ts`**
    Service registry module with:
    - `ServiceDefinition` interface: `{ slug: string; i18nKey: string; icon: LucideIcon }`
    - `SERVICE_DEFINITIONS` readonly array with all 6 services:
      - `{ slug: 'google-ads', i18nKey: 'googleAds', icon: Target }`
      - `{ slug: 'facebook-ads', i18nKey: 'facebookAds', icon: Share2 }`
      - `{ slug: 'tiktok-ads', i18nKey: 'tiktokAds', icon: Play }`
      - `{ slug: 'seo', i18nKey: 'seo', icon: Search }`
      - `{ slug: 'email-marketing', i18nKey: 'emailMarketing', icon: Mail }`
      - `{ slug: 'consultanta-marketing', i18nKey: 'consultanta', icon: Lightbulb }`
    - `getServiceBySlug(slug: string): ServiceDefinition | undefined`
    - Export `ServiceDefinition` type for reuse.

    **Step 3: Create `src/components/sections/Breadcrumb.tsx`**
    Reusable breadcrumb component following the pattern in RESEARCH.md:
    - Accept `items: readonly BreadcrumbItem[]` where `BreadcrumbItem = { label: string; href?: string }`
    - Render `<nav aria-label="Breadcrumb">` with `<ol>` and Lucide `ChevronRight` separators
    - Include `<script type="application/ld+json">` with BreadcrumbList schema
    - Use `var(--section-text-muted)` for inactive items, `var(--section-text)` for current
    - This is a server component (no 'use client') — schema and links are static.

    **Step 4: Create Services index page sections**

    **ServicesHero.tsx** — Follow `AboutHero.tsx` pattern exactly:
    - 'use client', SectionWrapper theme="dark", py-24 md:py-32
    - Overline ("SERVICII"), TextReveal h1 variant="word" trigger="scroll", subheading fade-up
    - Brand glow decorative div
    - i18n from `useTranslations('services')`: `index.hero.overline`, `index.hero.headline`, `index.hero.subheading`
    - This renders the ONLY h1 on the page.

    **ServicesGrid.tsx** — Service cards grid:
    - 'use client', SectionWrapper theme="dark"
    - SectionHeader with `index.grid.overline` and `index.grid.heading`
    - 7 service cards in bento grid (3 columns on lg, 1 on mobile)
    - Each card: Lucide icon (48px, grey), service name as h3, division badge (caption-size pill with burgundy bg), description (body-sm, muted), arrow link
    - Card data from i18n: `index.grid.items` array with `{ title, division, description, link, icon }` — icon field maps to the service's Lucide icon
    - Actually, simpler approach: iterate `SERVICE_DEFINITIONS` + one extra entry for "Dezvoltare Web" at position 0. For Dezvoltare Web, link to `/servicii` (the index page itself) since there is no dedicated sub-page per RESEARCH.md recommendation.
    - Hover: card lift + burgundy glow + arrow slides right (use SpotlightCard or custom hover)
    - GSAP stagger fade-up animation, 80ms delay between cards
    - First 2 cards span 2 rows on lg (bento visual variety per servicii.md spec)

    **ProcessSteps.tsx** — How We Work section:
    - 'use client', SectionWrapper theme="light"
    - SectionHeader with `index.process.overline`, `index.process.heading`, `index.process.description`
    - 4 steps in horizontal row (desktop) connected by dotted lines, vertical timeline on mobile
    - Each step: large number (display-lg, Burgundy, opacity-20), Lucide icon (MessageSquare, PenTool, Rocket, BarChart3), title h3, description
    - GSAP stagger slide-from-left animation, 150ms delay
    - Hover: number opacity increases, icon color shifts to burgundy

    **WhyChooseSection.tsx** — Two-column stats + differentiators:
    - 'use client', SectionWrapper theme="dark"
    - SectionHeader with `index.whyUs.overline`, `index.whyUs.heading`
    - Left column: 2x2 grid of stat cards using `CountUp` component (150+ Projects, 50+ Clients, 340% ROI, 24/7 Support)
    - Right column: numbered differentiator list with check icons, fade-up stagger
    - Mobile: stacked (stats first, then differentiators)

    **Step 5: Create `src/app/[locale]/servicii/page.tsx`**
    - Async server component following `despre-noi/page.tsx` pattern
    - `setRequestLocale(locale)` as first statement
    - `generateMetadata` with title "Servicii Marketing Digital | AceAgency Bucuresti" (from SEO doc), meta description, canonical URL, hreflang alternates (ro, en, x-default), Open Graph, Twitter Card
    - Compose: `<Breadcrumb>` + `<ServicesHero>` + `<ServicesGrid>` + `<ProcessSteps>` + `<WhyChooseSection>` + `<CTASection namespace="services" secondaryHref="/contact">`
    - Reuse existing `CTASection` from `home/CTASection.tsx` with `namespace="services"` for the services-specific CTA copy.

    **Step 6: Add i18n content to `ro.json` and `en.json`**
    Add `services` top-level namespace with:
    - `services.index.hero` — overline, headline, subheading
    - `services.index.grid` — overline, heading, items array (7 items: Dezvoltare Web + 6 services)
    - `services.index.process` — overline, heading, description, steps array (4 steps)
    - `services.index.whyUs` — overline, heading, stats array (4), differentiators array (5)
    - `services.cta` — heading, description, primary, secondary (for CTASection namespace="services")
    - `services.breadcrumb` — home label, services label

    Use realistic Romanian content derived from servicii.md spec and SEO document keywords. English translations for all keys.

    **Content for services index (from servicii.md spec):**
    - Hero: "Solutii Digitale Complete" / "Complete Digital Solutions"
    - Grid heading: "Servicii Specializate pentru Fiecare Nevoie"
    - Process: "Cum Lucram" with 4 steps (Analiza & Strategie, Design & Dezvoltare, Implementare & Lansare, Monitorizare & Optimizare)
    - WhyUs: "Partenerul Tau Digital de Incredere" with stats (150+ Proiecte, 50+ Clienti, 340% ROI Mediu, 24/7 Suport) and 5 differentiators
    - CTA: "Hai sa Incepem un Proiect" / "Let's Start a Project"
  </action>
  <verify>
    - `npm run build` completes with zero errors
    - `/ro/servicii` renders all 5 sections with Romanian content
    - `/en/servicii` renders all 5 sections with English content
    - All 7 service cards visible in ServicesGrid with correct icons
    - Each service card links to the correct sub-page URL (or /servicii for Dezvoltare Web)
    - Breadcrumb renders "Acasa > Servicii" with BreadcrumbList JSON-LD in page source
    - Page source has single H1, correct title tag, meta description
    - View source confirms hreflang tags for ro, en, x-default
  </verify>
  <done>
    Services index page at /servicii renders with hero, 7-card grid, process steps, why-choose-us, and CTA in both RO and EN. All service cards link to sub-pages. Breadcrumb with JSON-LD schema present. SEO metadata correct (title under 60 chars, hreflang, canonical).
  </done>
</task>

<task type="auto">
  <name>Task 2: Dynamic service sub-page route with all 6 Service section components and Google Ads as first complete sub-page</name>
  <files>
    src/components/sections/services/ServiceHero.tsx
    src/components/sections/services/ServiceFeatures.tsx
    src/components/sections/services/ServiceProcess.tsx
    src/components/sections/services/ServiceStats.tsx
    src/components/sections/services/ServiceFAQ.tsx
    src/components/sections/services/ServiceCTA.tsx
    src/app/[locale]/servicii/[slug]/page.tsx
    src/messages/ro.json
    src/messages/en.json
  </files>
  <action>
    **Step 1: Create 6 reusable Service section components in `src/components/sections/services/`**

    All components accept `serviceKey: string` prop and use `useTranslations('services')` with `t(`${serviceKey}.sectionName.key`)`.

    **ServiceHero.tsx** — Service sub-page hero:
    - 'use client', follow AboutHero pattern
    - SectionWrapper theme="dark", py-24 md:py-32
    - Accept `serviceKey: string` and `icon: LucideIcon` props
    - Overline from i18n, TextReveal h1 variant="word" trigger="scroll", description fade-up
    - Service icon rendered large (64px) with grey color, top-right decorative
    - Brand glow decorative div
    - This is the ONLY h1 on the sub-page. Must contain the primary keyword per SEO doc.

    **ServiceFeatures.tsx** — Benefits/features grid:
    - 'use client', SectionWrapper theme="light"
    - SectionHeader with overline + heading (h2)
    - 4-6 feature items from i18n `${serviceKey}.features.items` array
    - Each item: Lucide icon (from a mapped set or generic), title (h3), description
    - Grid: 2 columns on md, 3 on lg, 1 on mobile
    - Use SpotlightCard for each feature with hover spotlight effect
    - GSAP stagger fade-up, 80ms delay

    **ServiceProcess.tsx** — How this service works:
    - 'use client', SectionWrapper theme="dark"
    - SectionHeader with overline + heading (h2)
    - 3-5 numbered steps from i18n `${serviceKey}.process.steps` array
    - Each step: large decorative number (opacity-20), title (h3), description
    - Vertical timeline layout with connecting line on left
    - GSAP stagger slide-from-left animation

    **ServiceStats.tsx** — Service-specific metrics:
    - 'use client', SectionWrapper theme="dark"
    - SectionHeader with overline + heading (h2)
    - 4 stat items from i18n `${serviceKey}.stats.items` array
    - Use CountUp component for each stat (value, suffix, label)
    - 2x2 grid on md, 4 columns on lg, 1 on mobile

    **ServiceFAQ.tsx** — FAQ accordion with schema:
    - 'use client', SectionWrapper theme="light"
    - SectionHeader with overline + heading (h2)
    - Use shadcn Accordion (type="single" collapsible) for FAQ items
    - FAQ items from i18n `${serviceKey}.faq.items` array via `t.raw()`
    - AccordionTrigger: text-left text-lg font-semibold
    - AccordionContent: text-base text-muted-foreground
    - Include `<script type="application/ld+json">` with FAQPage schema built from items
    - Accordion max-w-3xl mx-auto for readable width

    **ServiceCTA.tsx** — Service-specific CTA:
    - 'use client', SectionWrapper theme="dark"
    - Follow CTASection pattern but within the services namespace
    - Light grey card (#EBEBEB), rounded-[36px], TextReveal h2 heading
    - Primary button links to /contact, secondary button to /contact (Calendly anchor)
    - Uses `${serviceKey}.cta.*` i18n keys

    **Step 2: Create `src/app/[locale]/servicii/[slug]/page.tsx`**
    - `generateStaticParams` returning all 6 slugs from SERVICE_DEFINITIONS
    - `generateMetadata` with:
      - Title from i18n `${serviceKey}.meta.title` (use exact titles from SEO doc)
      - Description from i18n `${serviceKey}.meta.description`
      - Canonical URL: `https://aceagency.ro/${locale}/servicii/${slug}`
      - hreflang alternates for ro, en, x-default
      - Open Graph and Twitter Card tags
      - Service JSON-LD schema (type: Service, provider: AceAgency Organization)
    - Page component:
      - Async, `await params`, `setRequestLocale(locale)`
      - `getServiceBySlug(slug)` — if not found, call `notFound()`
      - Compose: `<Breadcrumb>` (Acasa > Servicii > {Service Name}) + all 6 Service sections in order
      - Fragment wrapper (no extra `<main>` — layout provides it, same as About page)

    **Step 3: Add Google Ads i18n content to `ro.json` and `en.json`**
    Add `services.googleAds` namespace with all 6 sections + meta:
    - `meta.title`: "Servicii Google Ads | Agentie Google Ads Romania - AceAgency" (exact from SEO doc)
    - `meta.description`: Compelling 155-char max description with CTA
    - `hero`: overline "GOOGLE ADS", headline "Servicii Google Ads Romania — Campanii care Genereaza Rezultate", description about Google Ads services
    - `features`: 4-6 feature items (Search Ads, Display Ads, Shopping Ads, YouTube Ads, Performance Max, Remarketing)
    - `process`: 4 steps (Audit & Analiza, Strategie & Setup, Optimizare Continua, Raportare & Scale)
    - `stats`: 4 metrics (e.g., 340%+ ROI, 150+ Campanii, 50+ Clienti, 2M+ Clicks)
    - `faq`: 5-8 FAQ items specific to Google Ads (cost, duration, expected results, platforms, etc.)
    - `cta`: heading, description, primary/secondary buttons

    Content should be SEO-optimized using keywords from Specificatii-Tehnice-SEO-AceAgency.md.
    English translations for all keys.

    **Internal linking per SEO doc:** ServiceCTA links to /contact. ServiceFeatures or ServiceProcess should include 1-2 inline links to related services (e.g., Google Ads mentions Facebook Ads). Breadcrumb links to /servicii. This satisfies 3-5 internal links minimum.
  </action>
  <verify>
    - `npm run build` completes with zero errors and shows `/servicii/[slug]` pages as `○` (static)
    - `/ro/servicii/google-ads` renders all 6 sections with Romanian content
    - `/en/servicii/google-ads` renders all 6 sections with English content
    - FAQ accordion expands/collapses items, keyboard navigable (Tab + Enter/Space)
    - Breadcrumb shows "Acasa > Servicii > Google Ads" with correct links
    - Page source has single H1 with "Google Ads" keyword
    - View source has FAQPage JSON-LD and Service JSON-LD
    - View source has hreflang tags for ro, en, x-default
    - Title tag matches SEO doc exactly
    - Navigating to `/servicii/nonexistent` returns 404
  </verify>
  <done>
    Dynamic service sub-page route renders /servicii/google-ads with all 6 sections (hero, features, process, stats, FAQ accordion, CTA). FAQ accordion is keyboard accessible. FAQPage and Service JSON-LD schemas present. Breadcrumbs with BreadcrumbList schema. SEO metadata matches spec. All 6 slugs generate statically. Invalid slugs return 404.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` — zero errors, all service pages static (`○`)
2. Services index at `/ro/servicii` and `/en/servicii` — renders all 5 sections, 7 cards with links
3. Google Ads sub-page at `/ro/servicii/google-ads` and `/en/servicii/google-ads` — all 6 sections rendered
4. FAQ accordion keyboard test — Tab to first item, Enter opens, Tab to second, Enter opens (single collapsible)
5. Breadcrumbs on both pages with correct JSON-LD
6. Single H1 per page, heading hierarchy H1 > H2 > H3 (no skips)
7. Title tags under 60 chars, meta descriptions under 155 chars
8. hreflang alternates for ro + en + x-default on all pages
9. Internal links: each page has 3+ internal links
</verification>

<success_criteria>
- Services index page fully functional with all content and animations
- Google Ads sub-page fully functional as the template proof
- All 6 Service* section components reusable for remaining 5 services
- `npm run build` passes with all service pages as static output
- SEO requirements met: single H1, heading hierarchy, schema markup, meta tags, hreflang, breadcrumbs
</success_criteria>

<output>
After completion, create `.planning/phases/04-service-pages/04-01-SUMMARY.md`
</output>
